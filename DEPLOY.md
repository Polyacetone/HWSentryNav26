# HWSentryNav26 实车部署说明

本文档简单介绍如何在实车上部署HWSentryNav26，以及常用的工作流。这里只是提供一个参考思路，并不详细，具体看你设计。

本文章含人量100%，可放心食用。

## 1. 软硬件环境

### 1.1 硬件环境

我们的轮腿哨兵机械设计上并不复杂，只需要在轮腿步兵的头上倒置一个MID360雷达，然后想办法把NUC放在云台上就行。

之前踩过一个坑，可以注意一下：由于雷达采用倒置安装，接近水平面的点云数量会比较有限。因此机械设计支撑结构时，应尽量避免遮挡雷达的低仰角视场，特别是不要在某些方位角方向形成大面积遮挡。我们之前的碳板安装高度相对于雷达XY平面更高，会遮挡某些方向上接近水平面的墙面点云。如果雷达在某个方位角范围内长期（超过里程计的平滑窗口，目前我设置的是3秒）无法观测到特定方向的墙面约束，可能会导致定位退化。

倒置单雷达是传感器上比较简洁的方案，已经足够实现稳定的里程计。但是由于支撑结构遮挡了雷达的部分视野，动态避障效果其实并不理想。如果你能说服你们的机械装两个MID360对地面附近全覆盖会更好。虽然我在这个架构下并没有做过多雷达融合，但是理论上来说应该直接直接把点云拼起来就行，因为图优化里程计似乎不怎么吃时间同步，无论是两个雷达间还是雷达和IMU间。

以及有一个值得说明的点是，目前我们的里程计使用的是电控发送的IMU（来自H7上的BMI088），而不是雷达内置的IMU。好处是H7上的BMI088量程可配置，加速度计的量程可以调到24g（相比之下MID360自带IMU加速度计量程只有4g），这样在剧烈运动的时候不容易饱和。不过我们并没有严格测试过量程差异对里程计稳定性的影响，之前应该是使用过一段时间的雷达内置IMU的（因为电控干了，发的IMU有问题），日常使用并没有发现明显的不稳定。

我们使用的计算平台是华硕NUC 15 Pro（Ultra 9 285H，32GB内存）。我这套方案确实比较吃CPU，之前测试过自瞄+导航+决策一起打开能给所有核平等地吃到40%占用 ~~（主要是我吃的）~~ 。如果预算有限需要使用CPU性能更低的运算平台，可能就不能像我这么奢侈地浪费算力（x），自己做点优化吧。

### 1.2 软件环境

这一块应该没什么需要说的，`HWSentryNav26`和`HWSentryCommon26`里的README已经讲过环境配置了，有可能踩坑的地方也在部署提示里面提到了。

对了，你可能会注意到通讯协议中有`SyncRequest`、`SyncResponse`两个包，以及`RawGimbalIMU`和`GimbalStatus`中包含`sample_ticks`字段。这个其实主要是给自瞄用的，为了同步相机图像和云台姿态的时间戳。测试下来IMU时间同步对我的导航应该没有影响（毕竟我雷达也懒得开PTP同步）。

## 2. 从0开始的部署流程

### 2.1 基础设施

这里假设你的机械拓扑和通信拓扑与我一致。

首先找机械要到各个关节位置的变换，填进`tf_maintainer`的`params.yaml`中。如果你也想在里程计中使用下位机IMU，别忘记更新`small_glim`的话题与传感器配置。

然后找电控商定串口通讯协议，根据你的设计修改`serial_bridge`和`interfaces`包。也可以根据你们的控制器设计修改`utils/py/sim/wheel_leg_lqr_follow_sim.py`中的动力学部分。

### 2.2 建个草图

先把`small_glim`的建图模式打开，并记得关闭`use_mapping_trigger`（这个是让建图的启动和停止由当前比赛状态`comp_stage`决定，比赛开始启动建图，比赛结束停止建图）。打开串口、雷达、里程计节点，手开跑一圈，从`~/mapping/mapping_<时间戳>`目录里取`mapping.pcd`就行。

然后运行`utils/py/map/terrain_label_editor.py`。这个是生成地形标注的GUI工具，自己摸索一下就会用了。标完保存地形标注图的`msgpack`文件。

将你的全局点云（`pcd`）和全局地图（`msgpack`）放进`map_server/maps`文件夹，在参数文件中把地图文件名填进去。然后把全局点云（`pcd`）也放到`odom_localizer/maps`，并修改参数文件中的地图文件名和起点位姿。你可以看到这里有`blue`、`red`、`default`三种起始位姿，这个是从裁判系统接收的`robot_status`中获取的。至于怎么获取起点位姿，你可以从全局点云中选点看坐标，也可以用之前的地图标注工具选点看坐标。差不多就行。

### 2.3 系统辨识

把各个节点都打开（可以使用`full_launcher`），然后再开一个`utils/py/msg/nav_goal_relay.py`。准备好了就打开`utils/py/mpc/rec_identify_data.py`（`--tag`是给此次采样打标签用的）。然后**在平地上确保不要撞上任何障碍物**地乱点开车。腿长也要参与变化（可以动态变化，也可以分多个腿长多次录制）。激励一分钟应该就差不多够了，关掉后数据保存到`identify_data`文件夹中。

收集到足够多的数据后，运行`utils/py/mpc/identify_lpv_mimo.py`，会自动读取`identify_data`，输出结果到`identify_lpv_result`文件夹。核对一下曲线图中预测和实际的重合度。差不多的话就可以用输出的`kinematic_model.yaml`替换`nav_executor`中的模型参数了。

> [!TIP]
> 如果发现我的默认模型在你的车上存在严重的失配导致根本无法到达目标点，你可以先用`utils/py/msg/send_chassis_cmd_interactive.py`手动发指令开车并收集数据，跑一遍系统辨识流程得到一个bootstrap模型，然后再用这个模型跑一遍标准的系统辨识得到最终的模型参数。

### 2.4 地形调参

这个时候应该能够在平地上跑了。接下来可以调整`nav_executor`中和地形跨越有关的参数 ~~（懒得细讲了）~~ ，然后在不同地形上跑一跑看。

## 3. 赛场使用流程

本章将简单讲讲场上我是怎么使用的，仅供参考。

### 3.1 适应性训练

26赛季取消了哨兵建图环节。所以我比较倾向于在适应性训练的时候专门写一个到处乱跑的决策行为用来建图。

适应性训练的时候没有先验点云，但是有先验地图标注（照着场地STEP转出来的STL转出来的PCD画，转换工具在`utils/py/cloud`中，或者直接用我在DEMO文档中提供的图），所以我们需要在`odom_localizer`中把`cloud_filename`设置成空，这样就会只发布初始位姿（需要比较精确地手摆）。`map_server`的`cloud_filename`也要设置成空，会自动回退到使用法向量的粗糙动态避障，或者你不放心的话可以直接设置`bypass_dynamic_obstacle`把动态避障关掉。

不过我在国赛试过其实用分区赛的全局点云和地图标注也能跑。不过还是推荐在适应性的时候走一遍建图流程。不建议依赖自由调试阶段建图，离线优化虽然能去除动态障碍物，但是总有一些人一直站在那里当静态障碍物。

### 3.2 准备地图

走一遍离线优化流程，得到一张还算可以的全局点云。这个时候一定不要忘记把点云坐标系原点对齐到裁判系统的坐标系原点，即**红方补给区所在的那个角落**。对齐这个步骤可以先用`utils/py/cloud/transform_cloud_3pts.py`，然后用`utils/py/cloud/transform_cloud_manual.py`。

然后我们可能发现离线优化不能去除某些一直死在那里的车。这个时候可以使用`utils/py/cloud/clean_cloud_gui.py`和`utils/py/cloud/edit_cloud_gui.py`手动删掉障碍物。删完后如果地面是空的可以用`utils/py/cloud/draw_planes_gui.py`补上地面。 ~~（工具确实有点原始，不满意你可以自己写更好的）~~

接下来正常标注地形就行。这部分都一样。

### 3.3 测试全局定位

建好全局点云后，建议使用赛场录包测试一下全局定位是否有效。当时为了避免全局定位出现问题（毕竟GICP可能收敛到错误的局部最优），我就给`odom_localizer`加入了比较严格的验收条件。换个全局点云可能就需要调一下参数了，避免全被拒绝导致定不上位。

## 4. 一些提示

### 4.1 开发提示

- 项目在Debug和Release模式下使用不同的编译选项。Debug模式下启用了`-fsanitize=address,undefined`以帮助检测内存错误和未定义行为，但是非常影响性能。正常开发和测试时建议使用Release模式（`-DCMAKE_BUILD_TYPE=Release`），遇到问题时再切换到Debug模式（`-DCMAKE_BUILD_TYPE=Debug`）进行排查。

- 在Debug模式下使用LSAN时，可能会误报`rcl_node_init`函数的内存泄漏。这是由于ROS2的节点初始化过程中分配了一些全局资源，LSAN无法正确识别这些资源的生命周期。建议将`leak:rcl_node_init`加入LSAN的忽略文件（默认存放在工作空间下的`lsan.supp`），以避免误报。

### 4.2 部署提示

- Ubuntu系统下建议关闭NTP服务以避免系统时间跳变导致的里程计和TF异常：`sudo timedatectl set-ntp false`。

- 如果运行时遇到报错`Could not load library dlopen error: /lib/x86_64-linux-gnu/libpcl_io.so.1.14: undefined symbol: libusb_set_option, at ./src/shared_library.c:99`，这大概率是海康驱动干的。删除`/opt/MVS/lib/64/libusb-1.0.so.0`和`/opt/MVS/lib/32/libusb-1.0.so.0`即可（注意备份）。

- 可以关闭网卡节能模式以避免网络不稳定导致远程调车卡顿。`sudo nano /etc/NetworkManager/conf.d/default-wifi-powersave-on.conf`，将`wifi.powersave`的值改为2即可。