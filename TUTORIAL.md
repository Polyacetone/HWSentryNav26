# HWSentryNav26 导航入门指南

本文章由人类指导 LLM 编写并经过人工校对，含人量50%，可以放心食用。

## 目录

- [怎么读这份指南](#怎么读这份指南)
- [第 0 章 导航系统长什么样](#第-0-章-导航系统长什么样)
- [第 1 章 我在哪：定位问题](#第-1-章-我在哪定位问题)
- [第 2 章 周围有什么：地图问题](#第-2-章-周围有什么地图问题)
- [第 3 章 怎么走：规划问题](#第-3-章-怎么走规划问题)
- [第 4 章 怎么走稳：控制问题](#第-4-章-怎么走稳控制问题)
- [第 5 章 出错了怎么办：鲁棒性问题](#第-5-章-出错了怎么办鲁棒性问题)
- [第 6 章 跨地形：轮腿的终极问题](#第-6-章-跨地形轮腿的终极问题)
- [结尾：怎么继续探索](#结尾怎么继续探索)

## 怎么读这份指南

这份指南写给**有一定编程和 ROS2 基础、但对机器人导航还不熟悉**的新人。如果你连 ROS2 和 TF 都还没碰过，建议先花一两周把基础补上再回来。

先说明它**不是什么**：

- 它不是教材。不推导公式、不贴代码、不罗列术语定义。需要学的具体知识点，我只告诉你"该知道什么"，至于"怎么学"，现在这个时代你完全可以直接问 LLM。
- 它不是说明书。不告诉你哪个包怎么跑、哪个参数怎么调。这些事情你可以直接询问你的 coding agent。
- 它也不是"标准答案集"。恰恰相反，它想传达的第一件事是：**导航系统的每一个设计，都是被真实问题逼出来的**。没有人在动工之前就知道最终长什么样。

那它是什么？它是一个**思考框架**：把一套完整的导航系统拆成几个绕不开的问题，然后带你过一遍"遇到这个问题时，应该往哪个方向想"。我强烈建议你一边读一边保持提问：**"如果是我，我会怎么设计？"** 当你发现自己的方案和这个系统不一样时，别急着认为你错了——去想想为什么它选择了另一条路，往往那里藏着最有价值的东西。

还有一个心态上的建议：**走弯路是探索的正常部分，不用怕**。我做的每一个决定背后，几乎都踩过坑、返过工、推倒过重来。这篇文章写的不是"我避开了所有坑"，而是"坑帮我长成了现在的样子"。大胆去试，试错了不丢人。

## 第 0 章 导航系统长什么样

### 这一章解决什么问题

在动手写任何代码之前，先搞清楚一个机器人导航系统到底由哪些部分组成。如果对整体没有心智模型，你学到的每个知识点都是孤岛。

### 怎么想这个问题

一个能自主移动的机器人，本质上在做一件事：**循环回答四个问题**：

1. **我在哪？**（定位）
2. **周围有什么？**（地图）
3. **怎么走到目标？**（规划）
4. **怎么走得稳、走得安全？**（控制与容错）

这四个问题对应一条数据流：

```
传感器（雷达/IMU）→ 里程计 → 定位 → 地图 → 规划 → 执行 → 底盘指令
```

很多人第一次接触导航时犯的错误，是试图一次性理解整个系统。其实正确的姿势是**分层**：每一层只解决一个问题，层与层之间通过清晰的接口（坐标系、话题、数据契约）连接。分层有三个好处：**每一层可以独立替换和测试**、**每一层都有成熟的方法可以学**、**系统复杂度从"理解一个复杂系统"降为"理解几个小问题"**。

这里有一个很重要的判断：**什么时候该自研，什么时候该用现成框架？** 判断标准不是"别人都用"，而是"现成框架的抽象是否和你的问题对齐"。通用导航框架（比如经典的[Nav2](https://github.com/ros-navigation/navigation2)和大家喜闻乐见的[北极熊导航](https://github.com/SMBU-PolarBear-Robotics-Team/pb2025_sentry_nav)）非常优秀，它的分层思想和这里几乎一样——如果你的机器人是普通四轮、平地行驶，用它比较省心。但一旦你的需求超出它的抽象——比如地形不是"空地/障碍"二值、底盘动力学不是普通车模——你就要在它的框架里打补丁，补丁越多，越不如自己搭。这个仓库就是后者的例子：轮腿要跨台阶，通用框架给不了这个抽象。

一个反直觉的结论：**自研不是炫技，是当现成框架的抽象和你的问题不匹配时，成本最低的选择。** 特别是有了 coding agent 之后，手搓自研导航框架真的不是什么难事。而分层思想是两者通用的，学 Nav2 的分层结构和学本仓库的分层结构，收获是一样的。

### 需要了解的关键知识点

- **通用导航栈的分层结构**：Nav2 里的 costmap / planner / controller 各负责什么——对照本仓库的分层，理解"自研 vs 现成框架"各自适合什么场景
- **传感器数据形态**：LiDAR 点云和 IMU 各提供什么信息——这是数据流的源头，后面的每一层都从这两种数据出发

### 本仓库的体现

本仓库的四大包正好对应四个问题：`small_glim`（里程计）、`odom_localizer`（定位）、`map_server`（地图）、`nav_executor`（规划 + 执行）。整体数据流见 DESIGN.md 第 0 节。

## 第 1 章 我在哪：定位问题

### 这一章解决什么问题

机器人睁开眼睛，看到一堆点云——但它不知道自己在哪。定位是导航的地基：后面每一层都在某个坐标系里做决策，坐标系错了，一切都错。

### 怎么想这个问题

**位姿永远是相对的**——你必须先指定"相对于谁"，才能说"我在哪"。于是"我在哪"自然拆成两个小问题：

1. **我相对于起点走了多远？** 这是**里程计**回答的。它把起点作为原点（`odom` 系），高频输出机器人相对起点的位姿。
2. **我的起点在地图的哪里？** 这是**全局定位**回答的。它把里程计输出的点云和预建全局地图做配准，低频输出"起点在地图系下的位姿"——也就是 `map→odom` 这一个变换。

两者一合成，就得到机器人在 `map` 系下的位姿：

```
map 下的位姿 = map→odom 变换 ∘ odom 下的位姿
```

把这两个问题拆给两个独立节点，是导航框架里约定俗成的**低耦合**设计，好处很实在：

- **各自可以独立替换和测试**：里程计想换方案就换，全局定位想换配准方法就换，接口都只是"一个 TF 变换"；
- **没有地图时里程计照常工作**：在完全未知的环境里，只要相对起点不丢，导航就能动起来（先跑起来、再谈定位）；
- **各自的责任边界清晰**：里程计负责"高频、连续、不跳变"，全局定位负责"低频、绝对、过质量门"，一个出错不会连累另一个。

还有一个值得想清楚的点：**里程计的漂移从哪里来？** 现代激光里程计（LIO）采用 LiDAR-IMU 紧耦合估计：LiDAR 通过与局部地图（如 iVox 体素地图）进行配准（GICP/VGICP）提供环境约束，IMU 提供高频运动预测并辅助点云去畸变，两者在同一状态估计过程中相互校正。LIO 始终“观察”周围地形，因此不会像纯 IMU 那样快速累积漂移；但它只能约束相对于局部环境的运动，并不知道自己在全局地图中的绝对位置。长期运行中，配准误差、IMU 零偏、去畸变残差和传感器噪声等小误差仍会逐渐累积，形成里程计漂移。此外，在剧烈撞击、快速运动或传感器异常情况下，IMU 可能产生错误预测或饱和，导致点云去畸变和配准失败，使估计状态短时间内发散，里程计会直接原地起飞。

第二个要点：**选什么里程计方案，取决于你的工况有多恶劣**。这里有两大家族值得了解：

- **滤波框架（IEKF/ESKF）**：Fast-LIO2、Point-LIO 等主流 LIO 都属于这一类，计算效率高、实时性好。但滤波估计依赖**局部线性化假设**——系统状态必须始终在线性化点附近。轮腿哨兵在比赛中会被剧烈撞击（IMU 饱和）、翻车（视野受限、姿态突变），状态瞬间偏离线性化点很远时，滤波器容易收敛到错误状态；而且滤波只维护当前时刻的状态估计，历史信息被压缩进均值与协方差，一旦错了很难自己纠回来。
- **因子图框架（滑窗优化）**：在滑动窗口内同时维护多个历史位姿，保留 IMU 预积分因子和点云配准因子。当某帧估计失准时，优化器可以重新线性化、联合调整整段历史状态，把错误"拽回来"。代价是每帧要跑一次优化，对求解器实时性要求更高。**对于频繁撞击、翻车的轮腿哨兵，鲁棒性比单纯的快更重要。**

第三，全局定位最重要的一个判断：**正确性优先于频率。** 一次错误的定位更新，等于让机器人"瞬移"到错误位置——下游的规划、控制全部基于这个错误位置重算，比定位"慢一点"要糟糕得多。为了下游的“稳定定位”假设，每一拍更新都要过**质量门**：配准收敛了吗？各自由度上都有有效约束吗？约束均衡吗？具体可以用配准信息矩阵的谱性质来判断——最小特征值太小意味着某个方向没锁住，条件数过大意味着约束失衡（长走廊里沿走廊方向就是这样）。**知道自己不知道，比假装知道安全得多。**

### 需要了解的关键知识点

- **点云配准**：ICP / GICP / VGICP 的区别与适用场景——里程计和全局定位的核心算子
- **激光惯性里程计（LIO）**：LiDAR 与 IMU 是怎么紧耦合的——理解"配准 + 积分互相校正"
- **因子图框架 LIO**：滑窗优化、iSAM2 是什么——理解"滤波 vs 因子图"的鲁棒性差异
- **滤波框架 LIO**：ESKF / IEKF 的原理——与上一条对照，理解两种流派各自的代价
- **退化检测与配准质量判据**：信息矩阵、条件数如何反映约束质量——理解"质量门"为什么用信息矩阵的谱性质

### 本仓库的体现

`small_glim` 是基于因子图优化的 LiDAR-IMU 紧耦合里程计：固定滞后滑窗联合优化 IMU 预积分与 GICP-to-iVox 配准因子，并针对轮腿极端工况加入"撞击暂停 + 延迟插入"保护，避免 IMU 饱和帧污染局部地图。`odom_localizer` 是独立的 GICP 全局重定位，配准结果经信息矩阵谱判据验收、步长上限保护与 EMA 滤波平滑后，发布 `map→odom` 变换。详见 DESIGN.md 第 2 节。

## 第 2 章 周围有什么：地图问题

### 这一章解决什么问题

定位回答了"我在哪"，地图回答"我身边的世界长什么样、哪里能走、哪里危险"。

### 怎么想这个问题

先想清楚"地图到底是什么"。地图的本质是表达 **"空间位置 → 通行代价"这个函数** ——规划和控制需要反复查询"这里能不能走、走过去代价多大"。这个函数怎么表示？连续形式没有通用的好办法：你没法用一个公式描述任意地形上的任意代价。点云也不行：它是几何采样，本身不携带代价，回答"这片区域能不能通过"要做最近邻搜索和几何推理，又慢又绕。工程上最自然的答案就是**离散化**：把空间切成小格，每格存一个值。

为什么是栅格而不是别的？因为我们做的是 **2D 导航**——机器人可被近似为在平面上运动，2D 空间离散化就是栅格，本质上就是一张图片。这不是原理上的必然：3D 导航会离散成体素，也有人直接在点云上做规划。栅格只是"2D + 查询方便"这个组合下的自然选择：查询是 O(1) 的、和 2D 搜索算法天然契合、工程积累深厚。

**地图怎么来，决定了它怎么分层。** 对这个比赛场景有两个先验事实：场地地形赛前已知；动态障碍只有场上其他机器人。这两个事实直接决定了地图的结构：

- **静态层**：场地赛前已知，所以静态部分（地形、墙体、坡道）可以离线构建，而且应该离线构建——在线识别地形（"这是台阶还是坡？以什么速度能跨？"）是极难的感知问题，赛前既然能拿到准确答案，就没必要在线上为难自己。静态层因此是准确、完整、提前标注好的。
- **动态层**：其他机器人的位置赛前不知道，只能在线检测。动态层是实时、局部的，只回答"现在有什么在动"。

两层合并才是决策用的完整代价图：静态层说"哪里能走"，动态层在上面叠加"现在别去"。注意，**分层的依据是信息获取的方式，而不是"动静"本身**——如果场景变成未知环境探索，地图就得在线增量构建，静态和动态的边界会模糊。是"赛前已知"这个条件，让"提前标注静态部分"成为正确选择。

动态层的内容值得拆开想：**检测只是第一步，真正的问题是跟踪与预测。** 很多人以为"检测到障碍"就完事了，但检测只回答"这一帧有什么"，避障需要回答"它下一帧在哪、要去哪"。这一帧的散点怎么变成"对象"？——聚类。帧间的对象怎么对应？——数据关联（匈牙利匹配）。对象状态怎么平滑和外推？——滤波（卡尔曼）。有了航迹才能预测未来几秒的位置，把预测写进代价图，MPC 才能"看着"障碍的未来位置提前避让。这一整条链——提取 → 聚类 → 关联 → 滤波 → 预测——才是动态避障的真实形态。

代价地图上还有两个容易被混为一谈、但动机完全不同的处理：**膨胀**与**平滑**。

**膨胀**解决的是机器人的物理尺寸：机器人不是质点，它有轮廓和半径。与其在规划里处处考虑机器人的形状，不如把障碍物向外膨胀一个机器人半径——之后规划把机器人当质点处理，膨胀后的障碍边界就是"以质点为圆心、机器人半径为半径的圆不会碰到真实障碍"的安全边界。这个意义上的膨胀是**硬约束**：只要不进入膨胀区域，物理上就不会撞。

**平滑**则是为了下游的梯度优化器：规划和控制都是基于梯度的，它们靠"梯度"感知世界。如果代价只在障碍格内跳变、格外为 0，优化器从远处就"看不见"障碍（梯度为零的地方够不着），到边界又梯度突变（线搜索失败）。所以要把代价"抹"成连续的场：离障碍越近代价越平滑地升高。这同时带来了一个**软约束**：代价场让"离障碍远一点"天然更优，避免无意义地贴着障碍走。

再进一步，地图不只是"空地/障碍"二值。对这个仓库的主角，台阶、斜坡、飞坡是第三种东西：**可以走、但需要特定动作的地形**。它们是障碍吗？不是，车能过。它们是空地吗？不是，不能随便过，也不能停在里面。它们需要被标注出来：地形类型、穿越方向、允许的速度——这就是语义层。地图于是从"代价图"进化为"语义图 + 代价图"。这也解释了为什么静态部分值得离线标注：地形语义这种信息，在线识别很难，标注却很简单——赛前做一次，全场比赛受用。

### 需要了解的关键知识点

- **占据栅格地图**：栅格地图怎么表达环境、代价地图如何从传感器数据构建——机器人导航的地图理论基础
- **距离变换与距离场**：距离变换、SDF / ESDF 是什么——理解"膨胀"和"平滑"背后的数学工具
- **动态障碍物检测**：有哪些方法流派（背景差分、法向分析、多帧差分、语义分割等）——动态物检测是一个研究主题，不是某一招
- **多目标跟踪**：聚类、数据关联（匈牙利匹配）、卡尔曼滤波、航迹管理——把"这一帧有什么"变成"它要去哪"
- **运动预测**：恒速模型、衰减模型等预测未来位置的方法——预测障碍物走向的理论基础
- **语义地图**：语义地图与占据栅格的区别、语义信息怎么表示——让地图携带语义的理论基础

### 本仓库的体现

`map_server` 是地图层：加载离线标注的静态语义图（地形标签 + 方向场），叠加在线的动态障碍检测/跟踪/预测，输出"当前 + 未来 N 帧"的代价图序列供控制层消费。静态与动态的"信息获取方式"不同，处理链路也因此不同：静态层赛前标注好，动态层在线跑完整条"提取 → 跟踪 → 预测"链。详见 DESIGN.md 第 1 节。

## 第 3 章 怎么走：规划问题

### 这一章解决什么问题

给定起点和目标点，生成一条机器人真的能走的路。注意是"真的能走"——不是画一条线，而是考虑障碍、动力学、地形约束之后还能执行。

### 怎么想这个问题

规划最容易犯的错误是"一步到位"：在一个巨大的状态空间里直接搜索最优轨迹。位置、速度、朝向、时间全耦合，状态爆炸，慢且脆弱，出了问题还定位不了。

比较好的方法是**从粗到细、分阶段降维**——每一步只解决一类问题：

1. **拓扑**：从哪边绕过去、穿过哪个通道？（栅格搜索，比如 A*）
2. **几何**：一条像样的连续曲线（平滑、去折角）
3. **动力学**：以真实的速度/朝向约束，能不能走通？（带状态的搜索）
4. **优化**：在可行解附近打磨——更光滑、更贴约束（轨迹优化）
5. **时标**：何时快、何时慢（速度剖面）

每一阶段只解决一个问题，每个阶段都可以独立验证，任何一步失败都能快速定位。这是工程上的分而治之，也是数学上的降维。

这里有一个值得反复琢磨的判断时刻：**为什么需要动力学搜索？几何路径什么时候不够了？** 答案是当世界要求"以什么状态经过"时——比如上台阶：你得冲着一个方向、以特定的速度带起跳。几何路径只说了"经过哪里"，没说"以什么速度、什么朝向经过"。这个需求一出现，纯几何的规划就开始不够用了，需要带状态（速度/朝向）的搜索，以及在优化里加入速度、方向约束。举个例子，车在飞坡入口，加速空间不足以满足飞坡速度，此时空间最短路会完全忽视加速距离直接冲过去，但是动力学规划会拦截不可行速度窗，保留一个先后退取得加速空间，再冲过飞坡的可行解。

再想深一层：**搜索和优化是互补的**。搜索负责发散——找到"存在这样一条路"的大方向（它保证可行性）；优化负责收敛——在可行解附近打磨光滑和精细约束（它保证质量）。只搜不优，路粗糙到没法执行；只优不搜，优化器会陷在局部最优里出不来。好系统里两者分工明确：搜索给出种子，优化在种子附近精修。

这一章里还有两个值得展开的设计权衡。它们不是普世真理——理解"为什么在这里这么选"，比记住结论更重要：

- **约束的软硬跟着层职责走。** 一个看起来矛盾的例子：同一个台阶约束，在 A\* 和 KinoA\* 里是硬的（方向不对、速度窗口进不去，这个基元就不展开），在 MINCO 里却是软的（台阶速度窗、方向对齐都是代价项，不是硬等式）。这不是自相矛盾，而是两层分工不同：搜索层是离散的，它负责**裁决可行性**——"这条路能不能走通"，硬约束在这里只是剪枝，不展开不合法节点，没有任何数值代价，而且可行性必须在搜索层定下来，拓扑错了后面一切白搭；优化层是连续的，它负责**引导质量**——搜索已经保证了可行，优化只需要"尽量好"，而"尽量"天然就是软约束（代价项），用硬约束反而要处理可行性问题（约束处理、不可行重启）。所以规律是：**裁决可不行用硬约束，引导质量用软约束**。如果你在连续优化里见到硬约束，往往意味着这一层同时要承担裁决职责。
- **几何与时标分开做。** 这是本仓库的架构选择，不是唯一答案——像 MINCO 这类轨迹优化本身是支持路点和时长联合优化的。选择分开做，有两个挺工程化的动机：第一，MINCO 是 min-jerk 优化，能量项让轨迹天然光滑，但代价是速度往往不会顶到上限——min-jerk 最优意味着"温和"的速度曲线，而比赛想跑得快。单独做速度剖面，可以用"尽量快"这类直白目标把速度顶上去，还能顺带把底盘能力档、台阶速度窗这些约束放进来一起解决。第二，先有空间路径，才好做地形标注：台阶在哪、以什么模式过、速度窗多少，这些本质是"沿路径的函数"，几何定下来之后才能沿着弧长标注。两者再用**统一坐标（弧长）**缝起来，让下游只消费一个一致的表达。如果你的执行层没有这类需求，联合优化也完全可行——先想清楚"谁在消费时标"，再决定要不要解耦。

### 需要了解的关键知识点

- **A\***：启发函数是什么、双向搜索为什么快——拓扑搜索的经典算法
- **B 样条 / 曲线平滑**：为什么用 B 样条把折线变成光滑曲线——几何层的基本表达
- **state lattice / 运动基元**：怎么把连续状态离散成可枚举的格点与基元——带状态的搜索基础
- **Kinodynamic A\***：和普通 A\* 的区别、为什么需要带速度/朝向——理解"几何路径不够用"的时刻
- **轨迹优化（MINCO）**：分段多项式、微分平坦是什么——把搜索种子打磨成光滑可执行的轨迹
- **数值优化**：L-BFGS、非凸优化、代价光滑性——优化器能收敛的前提
- **速度剖面与弧长**：弧长域的速度规划、时间最优——理解"几何与时标分开做"的两个动机：min-jerk 轨迹不会顶满速度、地形标注依赖先有空间路径

### 本仓库的体现

`nav_executor` 里的规划器实现了完整的六段管线：空间 A*（拓扑）→ 参考路径 + 走廊（几何与引导）→ 走廊内状态格搜索（动力学）→ MINCO 优化（光滑与约束）→ 台阶标注（执行契约）→ 弧长域速度剖面（时标），最后统一打包成不可变路径包交给执行层。详见 DESIGN.md 第 3.3 节。

## 第 4 章 怎么走稳：控制问题

### 这一章解决什么问题

路径有了，接下来：怎么让底盘真的沿着走，而且走得稳？这是最"物理"的一章——控制器面对的是一台真实的、有惯性和延迟的机器。

### 怎么想这个问题

控制质量首先取决于**对模型的认知**。这是新手最常跳过的步骤——拿到路径就写 PID，然后开始调参，调不出来就怀疑人生。其实问题不在参数，在模型。

以这个仓库的主角为例：轮腿底盘是典型的**非最小相位系统**——指令阶跃后，实际速度会先往反方向冲一下再跟上（腿要先向后摆才能取得加速度），还伴随大延迟。这种对象，简单 PID 或者"理想模型"的控制器追不上，不是调参能解决的——**这是模型问题，不是参数问题**。

所以正确的路线是：

1. **先认识你的对象**：它的动力学特性是什么？延迟多大？有没有隐藏状态？
2. **用数据辨识模型**：别手搓。真实系统的模型比你想象的复杂（摩擦、耦合、随构型变化），录数据、拟合参数，让数据说话。
3. **不可测状态用观测器估计**：模型里有物理量你测不到或者难以表示（比如腿的隐藏动态），但不能不管它——用观测器从可测的输出里把它"重建"出来。
4. **在 MPC 框架里统一处理**：路径跟随同时要满足"贴路径、别撞、台阶限速、命令平滑"，这是多目标 + 约束 + 预测的问题——最优控制问题的标准形态，把所有这些目标放进一个优化问题里统一权衡。

到这里，整个思路可以浓缩成一个判断：**预测模型的真实性，决定控制质量的上限。** 这里"真实性"有两个常被忽略的层次。第一，模型要刻画真实系统本身——摩擦、延迟、隐藏动态，所以要辨识而不是手搓。第二，模型要吸收底层执行环节（内环）的闭环行为：上层控制器表达意图（去哪、多快），下层负责把意图变成电机动作，内环归内环——而预测模型反映的应该是"指令下去、车实际怎么动"这条完整链路，而不是理想化的响应。如果模型是假的，控制器就在错误假设上做最优决策——这就是"模型问题，不是参数问题"的完整含义。

但模型永远不完美，于是还剩两个绕不开的问题：**模型不完美怎么办？怎么短时间内把下一拍算出来？**

**第一个问题的答案：滚动时域，用"重算"换鲁棒。** 如果模型完美、未来确定，最优解应该是一次算到头的开环轨迹；但现实是模型有误差、环境在变。MPC 的答案不是把模型做完美，而是每拍只执行第一步、然后重新求解——"计划 → 执行一步 → 再计划"。这个循环本身就是反馈：误差和扰动只影响当前这一步，下一拍永远从当前位置重新出发。所以 MPC 的鲁棒性不来自精心设计的控制律，而来自"永远重来"这件事本身。想通这一点，你就明白为什么预测时域不必很长、模型不必完美——重算的频率和模型的"方向正确"，比绝对精度重要得多。

**第二个问题的答案：利用马尔可夫性质，用"结构"换速度。** 最优控制问题有一个其他优化问题没有的结构——链式（马尔可夫）结构：下一时刻的状态只依赖当前状态和控制，总代价是沿时间轴累加的。这允许把"整条轨迹的优化"分解成"一段一段"的问题：从最后一拍往前递推，每一拍只需要知道"从这个状态出发，未来最优代价是多少"（价值函数），而不必同时操作整条轨迹。DDP/iLQR 就是动态规划在连续控制里的形态：后向递推每步只处理局部信息，复杂度约 O(N·n³)（N 为时域步数、n 为状态维数）；而把整条轨迹当通用非线性优化（对 N·n 维向量求 Hessian、求逆）是 O(N³·n³)——差两个数量级。这也是为什么 MPC 的求解器通常是一族 DDP 而不是通用 NLP 求解器——不是后者做不到，是前者利用了问题的特殊形状。

但梯度方法有一个天生的局限，值得单独拿出来想：**它只能看到局部。** DDP 从当前解出发、沿着代价下降的方向迭代，如果代价地形是非凸的（比如障碍物左右各有一个可行的绕行方案），它就只会停留在出发时所在的那个"盆地"里——这就是规划里的同伦类问题。另一条完全不同的思路是**采样**：平行地掷出一大批候选控制序列，每条都前向仿真出一条轨迹，再按轨迹代价做加权平均——代价低的轨迹方向占主导，迭代几轮就向"整体上更优的方向"聚拢。这就是 MPPI。它不依赖梯度，所以不受"只能走下山路"的限制，能跨过局部最优；代价是采样效率低、输出带噪声、还要调温度和噪声方差这类参数。这类"采样 + 梯度"的组合（比如用 MPPI 给 DDP 提供初始解）是一个值得了解的扩展思路，但它不是免费的：采样要占算力、带噪声、还要调参，实际收益取决于场景——所以它更多是"需要跳出局部最优时的备选工具"，而不是控制器的默认配置。这里想让你带走的其实是那个更普适的分工：**采样负责发散找方向（该绕左还是绕右），梯度负责收敛求精度（在选定方向上打磨）**。这和上一章"搜索负责发散、优化负责收敛"是同一个思想在求解器尺度上的回响。

### 需要了解的关键知识点

- **纯追踪 / PID**：路径跟踪的基础算法、它的天花板在哪——理解"什么时候不够用"
- **MPC 思想**：模型 + 预测 + 滚动时域——理解"每拍重算、只执行第一步"为什么是鲁棒性的来源，而不是妥协
- **系统辨识**：ARX / LPV 模型是什么——理解"为什么模型要数据辨识而不是手搓"
- **观测器**：Luenberger / Kalman 怎么从可测输出重建不可测状态——模型里有测不到的物理量时怎么办
- **离散化**：ZOH 精确离散与朴素积分的区别——模型进控制器之前的最后一关
- **DDP 家族**：iLQR / DDP / FDDP 的区别、为什么马尔可夫链式结构让它们比通用 NLP 快——"用结构换速度"的求解器思维
- **MPPI**：采样式 MPC 的原理（平行采样 + 代价加权）、与梯度式 DDP 的互补关系——"发散找方向、收敛求精度"在求解器尺度的体现
- **非最小相位系统**：为什么轮腿速度会"先反向再跟上"——认识你的被控对象

### 本仓库的体现

`nav_executor` 的执行器：LPV 辨识模型 + 观测器 + 自研 FDDP 求解器 + 命令率控制 + 名义/辅助两层结构。三个问题共用一个求解器：跟随（Follow）、停车（Stop）、保持（Hold）。详见 DESIGN.md 第 4.3 节。

## 第 5 章 出错了怎么办：鲁棒性问题

### 这一章解决什么问题

前面四章假设一切顺利。但真实世界的导航会失败——卡住、贴障、没进展。怎么办？这一章讲的是"承认失败并优雅地处理失败"。

### 怎么想这个问题

首先要有一个心态转变：**"正常跟随会失败"是默认假设，不是悲观假设。** 车轮会卡在台阶边缘（有指令、没位移）、机器人会贴进障碍（当前位置已经危险）、路径会变得不可行（比如台阶被障碍物遮挡）。这些不是 bug，是物理世界的一部分。

然后要认清一个比赛机器人才有的奖惩结构：**撞一下、掉下去，可能还能继续打；但卡死在原地，就彻底失去作用。** 所以对比赛机器人，鲁棒性的第一性需求不是"安全"，而是**活性**——系统必须永远"活着"：出错可以，但必须还能动、还能继续前进。形式化一点说，系统性质分两类：安全性（safety，"坏事情永远不会发生"）和活性（liveness，"好事情终将发生"）。工业机器人出错可以停在安全状态等人来救（fail-safe）；比赛机器人不行——**停车等待本身就是死亡**。设计目标应该是 fail-live：就算出错，也出得还活着。记住这条主线，下面三条推论都是它的直接展开。

**推论 1：每个状态都必须有出口。** 状态机的死锁几乎都是同一个病根：某个状态的退出条件依赖一个可能永远不满足的信号——"等路径恢复"，但路径永远不会恢复了。所以设计状态机时，对每个状态问三个问题：怎么进？正常怎么出？**异常怎么出？** 第三个问题最容易漏，漏了就是潜在的不动点。脱困、危险恢复这类状态存在的意义不是"锦上添花的容错"，而是**死锁逃生通道**——它们保证任何状态下，系统都有一条"还活着"的路可走。

**推论 2：主动检测是打破不动点的机制，超时是保底出口。** 既然任何状态都可能因为条件不满足而卡住，就不能"等失败被发现"——要主动检测：无进展吗？卡住了吗？危险吗？但这里有一条必须成对的纪律：**检测必须和恢复成对出现**。只检测不恢复，判据反而制造新的死锁——检测到卡住，却没有地方可去，还是卡着。而超时的本质是"即使条件永远不满足，系统也能强制离开当前状态"——它给每个状态装了一个保底出口。判据与状态机的解耦（判据在外面算好、状态机只做决策转换）正是让这条纪律可行的结构：判据是"自知"的传感器，状态机是处理显式失败的决策器，两者各自独立演进、分别测试——判断是事实，决策是选择。

**推论 3：恢复链本身必须收敛。** 最容易被忽略的一层：恢复逻辑自己也会死锁——倒车 → 还是危险 → 再倒车 → 还是危险，循环往复。所以恢复链要像主状态机一样认真设计：第一，**先物理脱困，再重新规划**——卡住的位置起点不可行，直接重规划必然失败，必须先动起来（倒车、找安全点），恢复安全后再触发重规划；第二，**恢复最好有终态**——连续多次相近位置失败可以取消任务换个路径走，避免无限失败重试；第三，**不要触发不必要的恢复**——比如动态障碍挡路，绕开就行，不该触发恢复流程，否则恢复链被无关事件反复打断，本身就变成一种抖动。

到这里可以做一层澄清：**活性是硬约束，安全是软约束。** 安全约束（台阶动作不可打断、别撞墙）仍然要有，但它永远不能以失去活性为代价——卡住时宁可倒车撞到点什么，也不能卡死。错误代价的排序应该是：**卡死 > 致命 > 慢/绕**。台阶动作的"不可打断"看似是安全约束，其实并不违反活性：动作进行中系统还在动，只是不被打断。把"活着"设为硬约束、"安全"设为软约束，设计时的优先级就清楚了。

还有一个值得单独想的问题：**控制权仲裁**。底盘同时被多个需求争抢：正常跟随、停车、小陀螺（被打时的生存需求）、台阶动作（进行中绝不能被打断）。谁有权动路径？什么时候可以抢占、什么时候不可打断？仲裁的规则本质上也是在排序"哪个需求丢了会死"：小陀螺不转容易被打死（失活），所以它可抢占跟随（战术设计）；台阶打断会翻车（致命），但只是不被打断、系统仍然活着。这些规则必须显式写出来，而不是靠"碰巧没人抢"——仲裁不是控制器的细节，是系统级的语义。

最后，如果你觉得状态机繁琐，还有一个组织结构上的选择值得知道：**行为树**。状态机擅长表达强时序的流程（"必须经过这个状态才能到那个状态"），但状态一多转换图就爆炸，失败处理要单独画边。行为树把行为组织成树，每拍从根节点向下"tick"——选择节点按优先级尝试、顺序节点要求全部成功，**失败回退是树结构自带的语义**。它在游戏 AI 和 Nav2 里是主流，适合"任务流"：复杂任务拆成可复用的小行为，失败路径长在结构里。而状态机更适合"时序流"：台阶的"准备 → 锁定 → 执行 → 释放"有不可打断区间，用显式转换表达最清楚。所以不是谁更好，而是**你的行为是任务流还是时序流**——想清楚再选，比选了再想更省事。

### 需要了解的关键知识点

- **活性与安全性**：系统性质的两个经典分类——"好事情终将发生" vs "坏事情永远不会发生"——比赛机器人优先保证活性的理论基础
- **有限状态机（FSM）**：状态机设计模式、状态与转换的组织方式——组织"时序流程"型行为决策的经典结构
- **行为树（BT）**：Selector / Sequence 的语义、与状态机的取舍——组织"任务流"型行为决策的另一种结构（游戏 AI、Nav2 的主流选择）

### 本仓库的体现

`nav_executor` 的执行器：FSM 只管状态转换（正常/台阶/小陀螺/脱困/危险恢复/失能），所有判据在监控组件里算好；恢复链为"倒车 → 危险恢复 → 安全保持 → 强制重规划"；控制权仲裁三态（可用 / 锁当前 / 撤销）决定谁有权动路径。详见 DESIGN.md 第 4.2 节和第 3.1 节。

## 第 6 章 跨地形：轮腿的终极问题

### 这一章解决什么问题

对普通机器人，台阶是障碍——绕开。对轮腿机器人，台阶是要"跨"过去的。这个区别，是整个系统设计的引爆点：它迫使地图、规划、控制、接口全部升级。

### 怎么想这个问题

通用导航可以直接把台阶当障碍，因为它们的轮子过不去。轮腿要跨，所以必须把台阶从"障碍"里拎出来，承认它是第三种东西：**带动作的地形**。

这里值得先想清楚一个更深的问题：**为什么"障碍/空地"这个二分法装不下台阶？** 第 2 章说过，地图的本质是表达"空间位置 → 通行代价"的函数——普通代价地图把通行性写成位置的函数：这里能不能走。但台阶恰恰证明了这个抽象不够：**能不能过这个台阶，不取决于你在哪，而取决于你以什么状态经过**——朝向（要冲着台阶）、速度（要在速度窗里，快了翻车、慢了飞不过去）、底盘模式（要切换到跳跃）。所以方向场、速度窗、能力档这些看似"台阶特有的花活"，深层含义其实是：**地图从"位置 → 代价"的函数，进化为"位置 × 状态 → 代价"的函数**。普通 costmap 回答"这里能不能走"，语义地图回答"以什么状态走才可能"——多出来的每个维度（朝向、速度、模式），都对应一个真实会翻车的约束。想清楚这一点，你就明白地形语义不是锦上添花，而是跨地形导航的地基。

一旦承认这一点，整个系统都要跟着变——而且是从最底层变起：

- **地图要有语义**：标注地形类型和穿越方向（哪边是"上"、哪边是"下"），而不是只标"能不能走"。
- **规划要有约束**：要以某个速度带通过（飞坡时慢了可能掉坑里，下二级台阶时快了可能翻车）、要冲着台阶（方向对齐）、要提前助跑（起跳点可能在前面，不是物理边缘）。
- **接口要给底盘"模式"**：跨台阶是底盘的动作，导航不能只发速度指令，还要提前告诉底盘"准备跨台阶"，底盘切换到对应模式。
- **执行要有生命周期**：提前准备 → 模式切换 → 动作开始（此阶段不可打断！）→ 完成释放。因为底盘动作期间可能独占机构，导航的命令要配合它的节奏。

这里最值得带走的思想是**契约思维**：把"跨台阶需要什么"这个复杂的意图，在规划期就编译成一份**可执行的数据契约**——台阶在哪、什么模式、速度窗是多少、哪段不可打断——然后执行层只消费这份契约，不再重新解释地图。

为什么要这样设计？有两个层面的理由，一个比一个深。

第一，**执行层不应该"认识"台阶——知识放在数据里，而不是代码里。** 想象一下，如果执行层要自己解释地图，它就得内置一堆判断规则："前方是台阶 → 切换跳跃模式 → 速度保持在 1.6~2.2 m/s"。这些规则一旦写死在代码里，场地一变（换地图、改标注、调速度窗），就得改代码重新编译。契约的做法恰好相反：规划器把"台阶在哪、用什么模式、速度窗多少、哪段不可打断"全部算好，写进一份数据；执行层只读数据、照着做——它甚至不需要知道"台阶"这个词。场地变了，改的是地图和标注，代码一行不用动。这就是"执行层不解释地图"的真正含义：解释地图，等于把知识写死在代码里。

第二，**计算频率应该跟随数据的生命周期。** 台阶标注完全依赖全局路径：路径不变，标注就不会变。既然输入不变、输出也不会变，就没有理由每拍重算——把它放在规划期，路径一变它自然跟着重新编译；放在执行期每拍重算，纯粹是浪费。这其实是一条贯穿整个系统的暗线：静态地形赛前离线标注（输入赛前已知，算一次）、动态障碍在线检测（依赖实时点云）、台阶契约在规划期编译（依赖全局路径）、MPC 每拍求解（依赖底盘状态）——**"在哪里算、多久算一次"，由输入以什么频率变化决定**。

一句话总结：**规划层负责想清楚，执行层负责执行清楚**——这是复杂需求下保持系统清晰的唯一办法。

再往深看一层：方向场、速度窗、能力档、生命周期——这些不是台阶特有的花活，而是 **"把世界约束编进规划"的一般方法** 。你的机器人需要什么约束（无论是腿车上下台阶、飞坡、跳高地，还是饼车过洞），就把它编成地图语义和规划代价。这套方法可以迁移到任何非平面地形、任何特殊底盘。

### 需要了解的关键知识点

- **可通行性分析**：评估"地形 × 机器人状态"能否通行的通用方法——理解"通行代价依赖状态"的理论基础
- **可达性**：给定动力学与约束，哪些状态可达——"速度窗 / 能力档"这类约束背后的通用概念
- **混合系统与模式切换**：离散模式与连续动态的组合——"底盘模式 / 生命周期"背后的控制理论概念
- **数据驱动设计**：知识/配置放数据、逻辑保持通用——"契约思维"背后的通用软件原则

### 本仓库的体现

语义地图（地形标签 + 方向场）在 `map_server`；规划期的台阶约束（速度窗、方向对齐、助跑区）在 `nav_executor` 的规划器；执行期的生命周期契约（prepare → armed → committed → enter → exit → release，其中 committed 锁定路径）在执行器。详见 DESIGN.md 第 3.3 节和第 4.2 节。

## 结尾：怎么继续探索

这份指南到这里就结束了。最后说三件事：

**第一，把"为什么"当成你的习惯。** 读任何系统的代码时，先问"这里为什么这样设计"，再去验证你的猜测。这个仓库里每个"看起来过度设计"的地方，背后都有一个"当初那样不行"的故事——你不必知道故事，但一定要习惯提问。好设计的共同点不是"用对了什么算法"，而是**每个选择都有明确的动机**。

**第二，推荐的探索路径。** 先跑 DEMO.md 建立直觉（看东西动起来比读十篇文档有用）→ 按数据流顺序读 DESIGN.md（建立系统心智模型）→ 需要深入时再打开对应包名下的代码（或者直接问 coding agent）。知识类的问题，随时丢给 LLM 问，本文的"知识点"清单就是你的提问菜单。

**第三，也是最重要的：不要怕走弯路。** 这份指南里的每一个设计，都是从最朴素的版本开始，被真实问题一步一步逼出来的。第一个版本能有多简单就多简单——先让它跑起来，再让它跑得好。你会踩坑，会推翻自己的设计，会 ~~让 agent~~ 写出自己都看不懂的代码——这些都太正常了。**走弯路不是失败，是探索的必经之路。** 保持好奇，保持怀疑，一定要动手去做。