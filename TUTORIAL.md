# HWSentryNav26 导航入门指南

本文章由人类指导 LLM 编写并经过人工校对，含人量60%，可以放心食用。

## 目录

- [怎么读这份指南](#怎么读这份指南)
- [第 0 章 导航系统长什么样](#第-0-章-导航系统长什么样)
- [第 1 章 我在哪：定位问题](#第-1-章-我在哪定位问题)
- [第 2 章 周围有什么：地图问题](#第-2-章-周围有什么地图问题)
- [第 3 章 怎么走：规划问题](#第-3-章-怎么走规划问题)
- [第 4 章 怎么走稳：控制问题](#第-4-章-怎么走稳控制问题)
- [第 5 章 出错了怎么办：鲁棒性问题](#第-5-章-出错了怎么办鲁棒性问题)
- [第 6 章 跨地形：轮腿的终极问题](#第-6-章-跨地形轮腿的终极问题)
- [结尾：怎么继续探索](#结尾怎么继续探索)

## 怎么读这份指南

这份指南写给有一定编程和 ROS2 基础、但对机器人导航还不熟悉的新人。如果你连 ROS2 和 TF 都还没碰过，建议先花一两周把基础补上再回来。

先说明它不是什么：

- 它不是教材。不推导公式、不贴代码、不罗列术语定义。需要学的具体知识点，我只告诉你"该知道什么"，至于"怎么学"，现在这个时代你完全可以直接问 LLM。
- 它不是说明书。不告诉你哪个包怎么跑、哪个参数怎么调。这些事情你可以直接询问你的 coding agent。
- 它也不是"标准答案集"。恰恰相反，它想传达的第一件事是：**导航系统的每一个设计，都是被真实问题逼出来的**。没有人在动工之前就知道最终长什么样。

那它是什么？它是一个思考框架：把一套完整的导航系统拆成几个绕不开的问题，然后带你过一遍"遇到这个问题时，应该往哪个方向想"。我强烈建议你一边读一边保持提问：**"如果是我，我会怎么设计？"** 当你发现自己的方案和这个系统不一样时，别急着认为你错了——去想想为什么它选择了另一条路，往往那里藏着最有价值的东西。

还有一个心态上的建议：走弯路是探索的正常部分，不用怕。我做的每一个决定背后，几乎都踩过坑、返过工、推倒重来过。这篇文章写的不是"我避开了所有坑"，而是"坑帮我长成了现在的样子"。大胆去试，试错了不丢人。

## 第 0 章 导航系统长什么样

### 这一章解决什么问题

在动手写任何代码之前，先搞清楚一个机器人导航系统到底由哪些部分组成。如果对整体没有心智模型，你学到的每个知识点都是孤岛。

### 怎么想这个问题

一个能自主移动的机器人，本质上在做一件事，就是循环回答四个问题：

1. **我在哪？** （定位）
2. **周围有什么？** （地图）
3. **怎么走到目标？** （规划）
4. **怎么走得稳、走得安全？** （控制与容错）

这四个问题对应一条数据流：

```
传感器（雷达/IMU）→ 里程计 → 全局定位 → 地图 → 规划 → 执行 → 底盘指令
```

在往下之前，先划清一条边界：**目标点不是导航系统自己想出来的**。"去哪、为什么去"是战术问题，由一个独立的决策模块回答（在本仓库之外，通过话题把目标点发进来）。导航只负责两件事：把车安全地送到那个点，以及在这个过程中保护车自己。这条边界很重要——它意味着导航层不需要理解比赛，只需要理解运动。

很多人第一次接触导航时犯的错误，是试图一次性理解整个系统。其实正确的姿势是分层：每一层只解决一个问题，层与层之间通过清晰的接口（坐标系、话题、数据契约）连接。分层有三个好处：每一层可以独立替换和测试、每一层都有成熟的方法可以学、系统复杂度从"理解一个复杂系统"降为"理解几个小问题"。

这里有一个很重要的判断：什么时候该自研，什么时候该用现成框架？判断标准不是"别人都用"，而是"现成框架的抽象是否和你的问题对齐"。通用导航框架（比如经典的[Nav2](https://github.com/ros-navigation/navigation2)和大家喜闻乐见的[北极熊导航](https://github.com/SMBU-PolarBear-Robotics-Team/pb2025_sentry_nav)）非常优秀，它的分层思想和这里几乎一样——如果你的机器人是普通四轮、平地行驶，用它比较省心。

但一旦你的需求超出它的抽象——比如地形不是"空地/障碍"二值、底盘动力学不是普通舵轮/全向轮——你就要在它的框架里打补丁，补丁越多，越不如自己搭。这个仓库就是后者的例子：轮腿要跨台阶，通用框架给不了这个抽象。

一个反直觉的结论：**自研不是炫技，是当现成框架的抽象和你的问题不匹配时，成本最低的选择。** 特别是有了 coding agent 之后，手搓自研导航框架真的不是什么难事。而分层思想是两者通用的，学 Nav2 的分层结构和学本仓库的分层结构，收获是一样的。

### 一份理想化假设清单

如果下面这六条假设全部成立，导航就是一道简单作业题——A\* 一跑，速度指令一发，车就到了：

| 假设 | 内容 | 在哪一章倒下 |
| --- | --- | --- |
| A1 | 机器人始终知道自己在哪 | 第 1 章 |
| A2 | 环境完整已知，而且没有东西在动 | 第 2 章 |
| A3 | 机器人是个质点，没有尺寸和朝向，能瞬间改变速度 | 第 3 章 |
| A4 | 发什么速度指令，车就走什么速度 | 第 4 章 |
| A5 | 一切按计划发生，不会卡住、贴障、失效 | 第 5 章 |
| A6 | 地面是平的，"能走/不能走"是二值的 | 第 6 章 |

真实世界里它们一条都不成立。后面每一章的任务，就是推倒其中一条，然后看看系统被迫长出什么新东西。 **复杂度的产生一定有原因，也一定要有原因。**

### 需要了解的关键知识点

- **刚体变换与坐标系**：SE(2) / SE(3)、齐次变换矩阵、欧拉角与四元数、TF 树"坐标系即变换链"的语义——整个导航系统的通用语言，后面每一章都站在它上面
- **底盘运动学**：差速 / 全向 / 阿克曼 / 轮腿各自的运动学模型与能力边界——理解"为什么本仓库的规划和控制长这样"的第一块拼图
- **LiDAR 与 IMU 传感器原理**：测距原理与点云形态、IMU 的测量模型（零偏、噪声、Allan 方差）——数据流的源头，后面的每一层都从这两种数据出发
- **通用导航栈的分层结构**：Nav2 里的 costmap / planner / controller 各负责什么——对照本仓库的分层，理解"自研 vs 现成框架"各自适合什么场景
- **基础数学**：线性代数（矩阵、特征值、SVD）、概率与统计（高斯分布、MLE / MAP）——不是导航知识，但没有它，后面的条目大多读不懂

### 本仓库的体现

本仓库的四大包正好对应四个问题：`small_glim`（里程计）、`odom_localizer`（全局定位）、`map_server`（地图）、`nav_executor`（规划 + 执行）。整体数据流见 DESIGN.md 第 0 节。

## 第 1 章 我在哪：定位问题

> 这一章推倒：A1 机器人始终知道自己在哪。

### 这一章解决什么问题

稳定的定位是导航的地基。后面每一层都依赖于准确的定位信息，如果定位错了，一切都错。

### 怎么想这个问题

位姿永远是相对的——你必须先指定"相对于谁"，才能说"我在哪"。"我在哪"一般拆成两个小问题：

1. **我相对于起点走了多远？** 这是里程计回答的。它把里程计启动时的位置作为原点（`odom` 系），较高频（本系统中默认20Hz）输出机器人相对起点的位姿。
2. **我的起点在地图的哪里？** 这是全局定位回答的。它把里程计输出的点云和预建全局地图做配准，低频（本系统中默认1Hz）输出"起点在地图系下的位姿"——也就是 `map→odom` 这一个变换。地图的原点（`map` 系）是用来统一定位、全局地图、决策点位、裁判系统交互的锚点，应确保定义一致。本系统中，`map` 系的原点位于红方补给区所在的场地角落，x轴指向蓝方停机坪所在的场地角落，y轴指向红方停机坪所在的场地角落，z轴竖直向上。

两者一合成，就得到机器人在 `map` 系下的位姿：

```
map 下的位姿 = map→odom 变换 ∘ odom 下的位姿
```

把这两个问题拆给两个独立节点，是导航框架里约定俗成的低耦合设计，好处很实在：

- **各自可以独立替换和测试**：里程计想换方案就换，全局定位想换配准方法就换，接口都只是"一个 TF 变换"；
- **没有地图时里程计照常工作**：在完全未知的环境里，只要相对起点不丢，导航就能动起来（即全局配准可以轻松关闭，只需要发送固定的 TF 变换）；
- **各自的责任边界清晰**：里程计负责"高频、连续、相对"，全局定位负责"低频、准确、绝对"。

还有一个值得想清楚的点：里程计的漂移从哪里来？现代激光里程计（LIO）采用 LiDAR-IMU 紧耦合估计：LiDAR 通过与局部地图（如 iVox 体素地图）进行配准（GICP）提供环境约束，IMU 提供积分预测位姿并辅助点云去畸变，两者在同一状态估计过程中相互校正。

这里要区分两种完全不同的失效模式，别把它们混成一件事：

- **缓慢漂移** （正常状态下不可避免）：LIO 始终"观察"周围地形，因此不会像纯 IMU 那样快速累积误差；但它只能约束相对于局部环境的运动，并不知道自己在全局地图中的绝对位置。长期运行中，配准误差、IMU 零偏、去畸变残差和传感器噪声等小误差会逐渐累积。这是"漂移"的本义，也是为什么需要全局定位来兜住绝对位置。
- **瞬时发散** （异常状态下的崩溃）：剧烈撞击、快速运动或传感器异常时，IMU 可能饱和或给出错误预测，导致点云去畸变和配准同时失败，估计状态在很短时间内发散——里程计直接原地起飞。这不是"精度不够"，是估计器进了错误的盆地。

两者的应对手段也不同：前者靠全局定位周期性校正（也可以靠引入回环检测，但是 RM 场景下没有必要），后者靠里程计自己的鲁棒性、以及可以在异常发生时识别并保护（下面第二点的因子图鲁棒性、以及本仓库的"撞击暂停 iVox 更新"都是冲着它去的）。

第二个要点：选什么里程计方案，取决于你的工况有多恶劣。这里有两大家族值得了解：

- **滤波框架（IEKF/ESKF）**：Fast-LIO2、Point-LIO 等主流 LIO 都属于这一类，计算效率高、实时性好。但滤波估计依赖局部线性化假设——系统状态必须始终在线性化点附近。轮腿哨兵在比赛中会被剧烈撞击（IMU 饱和）、翻车（视野受限、姿态突变），状态瞬间偏离线性化点很远时，滤波器容易收敛到错误状态；而且滤波只维护当前时刻的状态估计，历史信息被压缩进均值与协方差，一旦错了很难自己纠回来。
- **因子图框架（滑窗优化）**：在滑动窗口内同时维护多个历史位姿，保留 IMU 预积分因子和点云配准因子。当某帧估计失准时，优化器可以重新线性化、联合调整整段历史状态，把错误"拽回来"。代价是每帧要跑一次优化，对求解器实时性要求更高。

对于频繁撞击、翻车的轮腿哨兵，鲁棒性比单纯的快更重要——所以本仓库选了后者。但要注意这个结论是工况推出来的，不是普适的：如果你的车不会翻、不会被撞飞，滤波方案省下来的算力可能更有价值。

第三，全局定位最重要的一个判断：**正确性优先于频率。** 一次错误的定位更新，等于让机器人"瞬移"到错误位置——下游的规划、控制全部基于这个错误位置，比定位"慢一点"要糟糕得多。为了下游的"稳定定位"假设，每一拍更新都要过质量门：配准收敛了吗？误差足够小吗？和全局点云重叠率足够高吗？是否和之前的定位出现跳变？各自由度上都有有效约束吗（看配准信息矩阵的最小特征值）？约束均衡吗（看配准信息矩阵的条件数）？**知道自己不知道，比假装知道安全得多。**

最后提醒一件容易被忽略的事：定位的正确性不只取决于算法，还取决于坐标系和时间戳这些"管道"。TF 树里的变换带着时间戳，下游查询的时刻晚于你发布的时刻就查不到；发布 TF 的线程如果和耗时的配准挤在一起，配准一慢 TF 就断流，下游全部瘫掉。这类问题的症状（"定位时好时坏"、"偶尔查不到变换"）离根因很远，排查时不要忽略它们。

### 需要了解的关键知识点

- **贝叶斯滤波与卡尔曼滤波**：从贝叶斯推断到 KF 再到 EKF——一切状态估计的地基，也是 IEKF / ESKF 和观测器（第 4 章）的前置
- **李群与李代数**：SO(3) / SE(3)、指数映射、扰动模型——现代激光里程计状态表示的标准语言，读任何 LIO 源码都绕不开
- **点云配准**：ICP / GICP / VGICP / NDT 的区别与适用场景——里程计和全局定位的核心算子
- **滤波式 LIO**：ESKF / IEKF、Fast-LIO / Point-LIO 等代表系统——"配准 + 积分互相校正"的滤波实现
- **因子图与图优化**：因子图建模、滑窗优化、iSAM2、IMU 预积分——"滤波 vs 因子图"鲁棒性差异的理论源头，因子图式 LIO（如 `small_glim`）的骨架
- **回环检测与位姿图**：回环描述子、位姿图优化——漂移的终极修正手段（本场景没有用，但要知道它的存在）
- **信息矩阵与配准质量判据**：协方差与信息矩阵、特征值与条件数——"质量门"的数学语言

### 本仓库的体现

`small_glim` 是基于因子图优化的 LiDAR-IMU 紧耦合里程计：固定滞后滑窗联合优化 IMU 预积分与 GICP-to-iVox 配准因子，并针对轮腿极端工况加入"关键帧延迟插入 iVox + 撞击时暂停 iVox 更新保护，避免 IMU 饱和帧污染局部地图。`odom_localizer` 是独立的 GICP 全局重定位，配准结果经信息矩阵谱判据验收、步长上限保护与 EMA 滤波平滑后，发布 `map→odom` 变换。详见 DESIGN.md 第 2 节。

## 第 2 章 周围有什么：地图问题

> 这一章推倒：A2 环境完整已知，而且没有东西在动。

### 这一章解决什么问题

定位回答了"我在哪"，地图回答"我身边的世界长什么样、哪里能走、哪里危险"。

### 怎么想这个问题

先想清楚"地图到底是什么"。地图的本质是表达"空间位置 → 通行代价"这个函数——规划和控制需要反复查询"这里能不能走、走过去代价多大"。这个函数怎么表示？连续形式没有通用的好办法：你没法用一个公式描述任意地形上的任意代价。点云也不行：它是几何采样，本身不携带代价，回答"这片区域能不能通过"要做最近邻搜索和几何推理，又慢又绕。工程上最自然的答案就是离散化：把空间切成小格，每格存一个值。

为什么是栅格而不是别的？因为我们做的是 2D 导航——机器人可被近似为在平面上运动，2D 空间离散化就是栅格，本质上就是一张图片。这不是原理上的必然：3D 导航会离散成体素，也有人直接在点云上做规划。栅格只是"2D + 查询方便"这个组合下的自然选择：查询是 O(1) 的、和 2D 搜索算法天然契合、工程积累深厚。

地图怎么来，决定了它怎么分层。对这个比赛场景有两个先验事实：场地地形赛前已知；动态障碍只有场上其他机器人。这两个事实直接决定了地图的结构：

- **静态层**：场地赛前已知，所以静态部分（地形、墙体、坡道）可以离线构建，而且应该离线构建——在线识别地形（"这是台阶还是坡？以什么速度能跨？"）是极难的感知问题，赛前既然能拿到准确答案，就没必要在线上为难自己。静态层因此是准确、完整、提前标注好的。
- **动态层**：其他机器人的位置赛前不知道，只能在线检测。动态层是实时、局部的，只回答"现在有什么在动"。

两层合并才是决策用的完整代价图：静态层说"哪里能走"，动态层在上面叠加"现在别去"。注意，分层的依据是信息获取的方式，而不是"动静"本身——如果场景变成未知环境探索，地图就得在线增量构建，静态和动态的边界会模糊。是"赛前已知"这个条件，让"提前标注静态部分"成为正确选择。

动态层的内容值得拆开想：**检测只是第一步，真正的问题是跟踪与预测。** 检测只回答"这一帧有什么"，避障需要回答"它下一帧在哪、要去哪"。这一帧的散点怎么变成"对象"？——聚类。帧间的对象怎么对应？——数据关联（匈牙利匹配）。对象状态怎么平滑和外推？——滤波（卡尔曼）。有了航迹才能预测未来几秒的位置，把预测写进代价图，MPC 才能"看着"障碍的未来位置提前避让。这一整条链——提取 → 聚类 → 关联 → 滤波 → 预测——才是动态避障的真实形态。

动态障碍物预测其实也有不同方法，并不局限于我这种显式提取、关联、跟踪的方法。我之前尝试过一种类似粒子滤波的方法，但是效果不佳：即跳过"对象"这一层，直接在栅格上做概率式的运动估计（给每个格子的占据状态配一群带速度的粒子，靠帧间的表观运动更新它们）。听起来更优雅——不需要聚类、不需要数据关联。但是效果不好，存在计算量大、粒子退化和发散等问题。除此之外还可以引入基于深度学习的方法，比如用 CNN 之类的。感兴趣的话你也可以尝试。

代价地图上还有两个容易被混为一谈、但动机完全不同的处理：膨胀与平滑。

膨胀解决的是机器人的物理尺寸：机器人不是质点，它有轮廓和半径。与其在规划里处处考虑机器人的形状，不如把障碍物向外膨胀一个机器人半径——之后规划把机器人当质点处理，膨胀后的障碍边界就是"以质点为圆心、机器人半径为半径的圆不会碰到真实障碍"的安全边界。这个意义上的膨胀是硬约束：只要不进入膨胀区域，物理上就不会撞。

平滑则是为了下游的梯度优化器：规划和控制都是基于梯度的，它们靠"梯度"感知世界。如果代价只在障碍格内跳变、格外为 0，优化器从远处就"看不见"障碍（梯度为零的地方够不着），到边界又梯度突变（可能导致线搜索失败）。所以要把代价"抹"成连续的场：离障碍越近代价越平滑地升高。这同时带来了一个软约束：代价场让"离障碍远一点"天然更优，避免无意义地贴着障碍走。

这两件事在实现上通常由同一次距离变换一起完成（算出每格到最近障碍的距离，近处给满代价、远处按距离衰减），但动机是两个，调参时要分开想：满代价半径管"会不会撞"，衰减段管"对优化器友不友好、有多愿意避开障碍物"。

再进一步，地图不只是"空地/障碍"二值。对这个仓库的主角，台阶、斜坡、飞坡是第三种东西：可以走、但需要特定动作的地形。它们是障碍吗？不是，车能过。它们是空地吗？不是，不能随便过，也不能停在里面。它们需要被标注出来：地形类型、穿越方向、允许的速度——这就是语义层。地图于是从"代价图"进化为"语义图 + 代价图"。这也解释了为什么静态部分值得离线标注：地形语义这种信息，在线识别很难，标注却很简单——赛前做一次，可以一直使用。

### 需要了解的关键知识点

- **占据栅格地图**：栅格地图怎么表达环境、log-odds 概率更新与反传感器模型——栅格地图"增量更新"的理论基础
- **距离变换与距离场**：距离变换、SDF / ESDF 是什么——理解"膨胀"和"平滑"背后的数学工具
- **点云分割**：地面提取、欧式聚类、区域生长——把"一堆点"变成"对象"的前置操作
- **动态障碍物检测**：有哪些方法流派（背景差分、法向分析、多帧差分、语义分割等）——动态物检测是一个研究主题，不是某一招
- **多目标跟踪**：聚类、数据关联（匈牙利匹配）、卡尔曼滤波、航迹管理——把"这一帧有什么"变成"它要去哪"
- **运动预测**：恒速模型、衰减模型等预测未来位置的方法——预测障碍物走向的理论基础
- **语义地图**：语义地图与占据栅格的区别、语义信息怎么表示——让地图携带语义的理论基础
- **体素地图**：OctoMap、iVox 等增量式局部地图——占据栅格之外的另一主流形态（`small_glim` 的局部地图就是 iVox）

### 本仓库的体现

`map_server` 是地图层：加载离线标注的静态语义图（地形标签 + 方向场），叠加在线的动态障碍检测/跟踪/预测，输出"当前 + 未来 N 帧"的代价图序列供控制层消费。静态与动态的"信息获取方式"不同，处理链路也因此不同：静态层赛前标注好，动态层在线跑完整条"提取 → 跟踪 → 预测"链。详见 DESIGN.md 第 1 节。

## 第 3 章 怎么走：规划问题

> 这一章推倒：A3 机器人是个质点，没有尺寸和朝向，能瞬间改变速度。

### 这一章解决什么问题

给定起点和目标点，生成一条机器人真的能走的路。注意是"真的能走"——不是画一条线，而是考虑障碍、动力学、地形约束之后还能执行。

### 怎么想这个问题

规划最容易犯的错误是"一步到位"：在一个巨大的状态空间里直接搜索最优轨迹。位置、速度、朝向、时间全耦合，状态爆炸，性能开销巨大且脆弱，出了问题还定位不了。

比较好的方法是从粗到细、分阶段降维——每一步只解决一类问题：

1. **拓扑**：从哪边绕过去、穿过哪个通道？（栅格搜索，比如 A*）
2. **几何**：一条像样的连续曲线（平滑、去折角）
3. **动力学**：以真实的速度/朝向约束，能不能走通？（带状态的搜索）
4. **优化**：在可行解附近打磨——更光滑、更贴约束（轨迹优化）
5. **时标**：何时快、何时慢（速度剖面）

每一阶段只解决一个问题，每个阶段都可以独立验证，任何一步失败都能快速定位。这是工程上的分而治之，也是数学上的降维。

再想深一层：搜索和优化是互补的。搜索负责发散——找到"存在这样一条路"的大方向（它保证可行性）；优化负责收敛——在可行解附近打磨光滑和精细约束（它保证质量）。只搜不优，路粗糙到没法执行；只优不搜，优化器会陷在局部最优里出不来。好系统里两者分工明确：搜索给出种子，优化在种子附近精修。

### 一个真实的架构演进：问题暴露的层次，不一定是它该被解决的层次

这一节讲一段我之前走过的弯路，因为它比任何抽象论述都更能说明"降维"这件事的分界线在哪。

我最早的规划链是：空间 A\*（只搜位置）→ 空间 B 样条平滑 → MPC 跟随。在平地上它工作得很好。随着要支持的地形越来越多，出现了一个怎么调都调不好的场景：车停在飞坡前面，离入口太近，加速距离不够。飞坡有速度下限——慢了会掉进坑里。这时正确的行为是先倒退一段取得加速空间，再冲上去。

我先在控制层找办法。MPC 理论上有前瞻，应该能自己发现"先退后进"更优，但它就是不产生这个行为。我当时的猜测是陷入了局部最优：往前是代价下降方向，往后是代价上升方向，梯度法自然不肯往后走。于是我尝试在控制层引入全局搜索来跳出局部最优——先试 MPPI（采样一大批控制序列取加权最优，把最好的那条交给梯度法精修），又试 CMA 一类的演化搜索。经过几个月的探索，行为可以初步涌现，但是非常不稳定。

在控制层反复折腾了很久，后来才想到这个问题在搜索层更容易解决，就是把运动学放进全局搜索。搜索状态从"位置"扩成"位置 + 朝向 + 速度"，边不再是格点间的直线而是真实可执行的运动基元。这样"先后退取得加速空间，再进入地形"就成为搜索空间里一条可以被显式搜出来的路径——它和"直接冲过去"作为两个候选被摆在一起比较，而后者会因为速度窗不满足被直接剪掉。

从这段试错里我拿到的最有用的一条判断是：**问题暴露的层次，不一定是它该被解决的层次。** 现象出现在控制层（MPC 不产生某个行为），根因却在搜索层（表示里缺一个维度）。遇到"下游怎么调都不对"的时候，先别急着换下游的算法，先问上游给的候选集里到底有没有你想要的那个解。如果没有，下游再强也只是在错误的集合里找最优。

这件事还有一个后续：搜索层解决之后，控制层那些为了跳出局部最优而引入的机制就全部失去了理由，最后都从仓库里删掉了。第 4 章会讲这个删除的完整判断过程。

这也是为什么前面那个"从粗到细"的分阶段列表里，第 3 步（动力学）是绕不过去的：当世界开始要求"以什么状态经过"时，纯几何的规划就不够了。上台阶要冲着一个方向、以特定速度带起跳；几何路径只说了"经过哪里"，没说"以什么速度、什么朝向经过"。

### 三个值得展开的设计权衡

它们不是普世真理——理解"为什么在这里这么选"，比记住结论更重要。

#### 约束的软硬跟着层职责走

一个看起来矛盾的例子：同一个台阶约束，在 A\* 和 Kinodynamic A\* 里是硬的（方向不对、速度窗口进不去，这个基元就不展开），在 MINCO 里却是软的（台阶速度窗、方向对齐都是代价项，不是硬等式）。这不是自相矛盾，而是两层分工不同。

搜索层是离散的，它负责裁决可行性——"这条路能不能走通"。硬约束在这里只是剪枝，不展开不合法节点，没有任何数值代价，而且可行性必须在搜索层定下来，拓扑错了后面一切白搭。优化层是连续的，它负责引导质量——搜索已经保证了可行，优化只需要"尽量好"，而"尽量"天然就是软约束（代价项），用硬约束反而要处理可行性问题（约束处理、不可行重启）。

所以规律是：**裁决可不可行用硬约束，引导质量用软约束**。如果你在连续优化里见到硬约束，往往意味着这一层同时要承担裁决职责。反过来，硬约束用错了地方会直接杀掉可行性——这一点第 6 章还会再遇到一次。

#### 几何与时标分开做

这是本仓库的架构选择，不是唯一答案——像 MINCO 这类轨迹优化本身是支持路点和时长联合优化的。选择分开做，有两个挺工程化的动机。

第一，MINCO 是 min-jerk 优化，能量项让轨迹天然光滑，但代价是速度往往不会顶到上限——min-jerk 最优意味着"温和"的速度曲线，而比赛想跑得快。单独做速度剖面，可以用"尽量快"这类直白目标把速度顶上去，还能顺带把底盘能力档、台阶速度窗这些约束放进来一起解决。

第二，先有空间路径，才好对路径做地形标注：台阶在哪、以什么模式过、速度窗多少，这些本质是"沿路径的函数"，几何定下来之后才能沿着弧长标注。

两者再用统一坐标（弧长）缝起来，让下游只消费一个一致的表达。如果你的执行层没有这类需求，联合优化也完全可行——先想清楚"谁在消费时标"，再决定要不要解耦。

#### 朝向该不该是独立的优化变量

这是一个真实的两难，我在两边都待过。

差速底盘和阿克曼底盘有一个本质区别：它可以零速自转，朝向和位置在原理上是解耦的。所以我最初把朝向 θ 当成独立的优化变量，和位置 (x, y) 并列——这个选择不是疏忽，它忠实地反映了底盘的真实能力，原地转向、边走边调朝向这类动作都能自然表达出来。

代价出现在求解上。θ 独立意味着优化器可以给出任何朝向，包括物理上做不到的（比如高速行驶时朝向和速度方向差 90 度）。于是"朝向要和运动方向一致"这个关系必须显式地压住，我用了增广拉格朗日：多一层外循环、多一组乘子、收敛质量明显变差。而这份约束在绝大多数时刻其实是紧的——差速车真正需要解耦朝向的场合并不多。

所以我换成了微分平坦表示：只优化位置 (x, y)，朝向由 $θ = atan2(ẏ, ẋ)$ 算出来。约束从此恒等成立——不是被压住的，是不可能违反。外循环删掉，变量数变少，收敛立刻好转。**但这确实丢掉了能力**：零速自转在这个表示里根本无法表达（速度为零时 atan2 没有定义），边走边独立调朝向也没有了。

这就是权衡的实质：**用表示能力换求解质量**。判断它是否划算，不能只看"丢掉了什么能力"，而要看**丢掉的那部分能力，在你的任务里到底什么时候被真正需要**。零速自转真正有用的场合其实很集中：起点和终点的朝向对齐。而这两处在本仓库里都不需要规划器负责：

- 终点没有朝向约束。目标就是一个二维位置，到了就算到了。哨兵的自瞄由云台负责，车体朝哪不重要。
- 起点的不对齐由搜索层吸收。Kinodynamic A\* 除了用起点朝向作为主根，还会在整圆上再撒几个不同朝向根，代价上给一点偏置。所以"当前朝向不利，需要先转个身"这件事在搜索层就被表达成了"从另一个朝向根出发"，不需要在优化层留一个自由度。
- 残余的对齐误差交给控制器。MPC 的代价里有朝向跟踪项，而且速度参考用的是有向投影 $v·cos(θ−θ_{path})$——朝向偏离路径时投影速度自然变小，控制器会先把车转过来再加速。对齐是控制层的自然行为，不需要规划层显式安排。

三条加起来，被微分平坦砍掉的那部分表示能力，恰好落在了任务不需要或别的层已经覆盖的地方。这才是我接受它的真正理由——不是"消冗余总是对的"，而是**这个具体的机器人、这个具体的任务里，那个自由度的价值不足以抵偿它带来的求解代价**。

### 为什么你的优化器有时效果不好

这一节是我认为整一章里最实用的部分，因为它对应的是你（手搓优化问题和求解器时）大概率会遇到、而且可能占掉大量时间的处境：管线搭好了，优化器却输出一团扭成麻花的东西，或者根本不收敛，或者时好时坏。

新手这时的第一反应通常是调权重。但大多数情况下，问题不在权重，而在于你交给优化器的那个问题本身——它可能是不可导的、量纲混乱的，或者起点就在错误的盆地里。下面三类是我实际撞过的根因，按"先怀疑什么"排序；前两类表现为解不出来，第三类表现为解出来了但不对。

#### 症状：线搜索反复失败 → 怀疑代价不连续

梯度优化器有一个隐含假设：代价函数是光滑的。任何形如 `if (label >= SLOPE) cost += ...` 的硬判断都会破坏它——采样点跨过那个边界时，代价瞬间跳变，梯度在跳变处不存在。线搜索沿着一个方向试探，撞上代价断崖就会不断缩小步长直到放弃。解法是把开关换成平滑门控（用 smoothstep 之类的过渡函数，在一小段区间内从 0 连续升到 1），让"是否生效"本身也可导。这一条和第 2 章"代价场要平滑"是同一个道理，只是那里针对地图，这里针对逻辑判断。

#### 症状：收敛缓慢 → 怀疑量纲

如果你的决策变量里既有位置（米）又有时间（秒）又有占据代价（无量纲），它们的梯度分量就不在同一个尺度上。优化器看到的是一个纯数字向量，它会优先在数值最大的方向上动——那个方向可能只是单位选得比较"小"而已。解法是分块缩放：告诉优化器每一块变量的物理尺度（位置块 0.5 米、时间块 0.1 秒），让它在无量纲坐标里维护历史和步长。

#### 症状：收敛了，但结果明显不对 → 怀疑种子

这一条和前两条不同：优化器没有报错，迭代正常退出，代价也在下降——它只是收敛到了一个你不想要的解上。最典型的形态是初始种子穿过了障碍物。

直觉上会觉得"把穿墙的代价调大它就会绕开"，但这不成立。想一下路径点落在墙里面时它感受到的梯度：占据代价是沿着墙的法向下降的，所以梯度会把点往墙的两侧推——路径被撑开、贴着墙的两边鼓出去，但整条路径的**拓扑没有变**，它依然穿墙。代价调得再大也只是让它撑得更用力，因为"绕过墙的这一端"意味着某个路径点要走一条代价更高的中间路线，那是梯度下降永远不会主动选择的方向。

这就是局部梯度优化器的盲目性：它只知道脚下的坡往哪边倒，不知道山那边有更好的地方。所以**保证种子落在可行域内、拓扑正确，是优化器的前置条件，不是它的职责**。这也是本章开头那条分工的另一面：搜索层的产出不只是"一条大致的路"，而是给优化器**指定了一个正确的同伦类**。如果你的优化结果时好时坏，先去看那次的种子长什么样，往往比调权重快得多。

#### 小结

把这三条合起来看，有一条贯穿的线值得单独记住：**"让优化器能收敛"不是优化器自己的事，而是一个跨层的系统性要求。** 地图层要提供平滑可导的代价场（第 2 章），规划层要给出拓扑正确的种子、选对约束的软硬（本章），控制层要保证代价项量纲一致（第 4 章）。任何一层偷懒，最后都会以"优化器不好用"的形式在下游爆出来——而那时你排查的位置离病根已经很远了。

### 需要了解的关键知识点

- **图搜索家族**：Dijkstra、A\*（启发函数的可采纳性与一致性）、D\*、JPS——拓扑搜索的经典谱系
- **采样式规划**：RRT / RRT\* / PRM 的原理与适用场景——另一条路线，理解"搜索 vs 采样"的取舍（本仓库没有用采样，但它是规划世界的半壁江山，了解即可）
- **曲线平滑**：可见性（LOS）平滑怎么把栅格折线抽稀成直线段、B 样条之类的参数曲线怎么把折线变成光滑曲线——几何层的基本表达，前者是本仓库的做法，后者是另一类常见选择
- **state lattice / 运动基元**：怎么把连续状态离散成可枚举的格点与基元——带状态的搜索基础
- **Kinodynamic A\***：和普通 A\* 的区别、为什么需要带速度/朝向——理解"几何路径不够用"的时刻
- **轨迹优化（MINCO）**：分段多项式、微分平坦是什么——把搜索种子打磨成光滑可执行的轨迹；也顺带理解"朝向该不该独立"那条权衡里，微分平坦到底换掉了什么
- **数值优化**：L-BFGS、线搜索（Armijo / Wolfe 条件）、非凸优化、代价光滑性——理解"为什么不收敛"的前提
- **凸优化与 QP**：凸集与凸函数、二次规划——走廊这类"约束 + 光滑"结构的数学基础

### 本仓库的体现

`nav_executor` 里的规划器实现了完整的六段管线：空间 A*（拓扑）→ 参考路径 + 走廊（几何与引导）→ 走廊内状态格搜索（动力学）→ MINCO 优化（光滑与约束）→ 台阶标注（执行契约）→ 弧长域速度剖面（时标），最后统一打包成不可变路径包交给执行层。详见 DESIGN.md 第 3.3 节。

## 第 4 章 怎么走稳：控制问题

> 这一章推倒：A4 发什么速度指令，车就走什么速度。

### 这一章解决什么问题

路径有了，接下来：怎么让底盘真的沿着走，而且走得稳？这是最需要控制论知识的一章——控制器面对的是一台真实的、有惯性和延迟的机器。

### 怎么想这个问题

控制质量首先取决于对模型的认知。这是新手最常跳过的步骤——拿到路径就写 PID，然后开始调参，调不出来就怀疑人生。其实问题不在参数，在模型。

以这个仓库的主角为例：轮腿底盘是典型的非最小相位系统——指令阶跃后，实际速度会先往反方向冲一下再跟上（腿要先向后摆才能取得加速度），还伴随大延迟和超调。这种对象，简单 PID 或者"理想模型"的控制器追不上，不是调参能解决的——这是模型问题，不是参数问题。

所以正确的路线是：

1. **先认识你的对象**：它的动力学特性是什么？延迟多大？有没有隐藏状态？
2. **用数据辨识模型**：别手搓。真实系统的模型比你想象的复杂（摩擦、耦合、随构型变化），录数据、拟合参数，让数据说话。
3. **不可测状态用观测器估计**：模型里有物理量你测不到或者难以表示（比如腿的隐藏动态），但不能不管它——用观测器从可测的输出里把它"重建"出来。
4. **在 MPC 框架里统一处理**：路径跟随同时要满足"贴路径、别撞、台阶限速、命令平滑"，这是多目标 + 约束 + 预测的问题——最优控制问题的标准形态，把所有这些目标放进一个优化问题里统一权衡。

到这里，整个思路可以浓缩成一个判断：**预测模型的真实性，决定控制质量的上限。** 这里"真实性"有两个常被忽略的层次。第一，模型要刻画真实系统本身——摩擦、延迟、隐藏动态，所以要辨识而不是手搓。

第二，模型要吸收底层执行环节（内环）的闭环行为：上层控制器表达意图（去哪、多快），下层负责把意图变成电机动作，内环归内环——而预测模型反映的应该是"指令下去、车实际怎么动"这条完整链路，而不是理想化的响应。如果模型是假的，控制器就在错误假设上做最优决策——这就是"模型问题，不是参数问题"的完整含义。

但模型永远不完美，于是还剩两个绕不开的问题：模型不完美怎么办？怎么短时间内把下一拍算出来？

#### 滚动时域：用"重算"换鲁棒

如果模型完美、未来确定，最优解应该是一次算到头的开环轨迹；但现实是模型有误差、环境在变。MPC 的答案不是把模型做完美，而是每拍只执行第一步、然后重新求解——"计划 → 执行一步 → 再计划"。这个循环本身就是反馈：误差和扰动只影响当前这一步，下一拍永远从当前位置重新出发。所以 MPC 的鲁棒性不来自精心设计的控制律，而来自"永远重来"这件事本身。想通这一点，你就明白为什么预测时域不必很长、模型不必完美——重算的频率和模型的"方向正确"，比绝对精度重要得多。

#### 马尔可夫结构：用"结构"换速度

最优控制问题有一个其他优化问题没有的结构——链式（马尔可夫）结构：下一时刻的状态只依赖当前状态和控制，总代价是沿时间轴累加的。这允许把"整条轨迹的优化"分解成"一段一段"的问题：从最后一拍往前递推，每一拍只需要知道"从这个状态出发，未来最优代价是多少"（价值函数），而不必同时操作整条轨迹。

DDP/iLQR 就是动态规划在连续控制里的形态：后向递推每步只处理局部信息，复杂度约 $O(N \cdot n^3)$（N 为时域步数、n 为状态维数）；而把整条轨迹当通用非线性优化（对 N·n 维向量求 Hessian、求逆）是 $O(N^3 \cdot n^3)$。代入本仓库的实际规模（N=40 步、n=12 维）算一下：前者约 7 万，后者约 1.1 亿，差了三个数量级。这就是为什么 MPC 的求解器通常是一族 DDP 而不是通用 NLP 求解器——不是后者做不到，是前者利用了问题的特殊形状。

### 梯度只能看到局部，谁来负责跨过去

梯度方法有一个天生的局限：它只能看到局部。DDP 从当前解出发、沿着代价下降的方向迭代，如果代价地形是非凸的（比如障碍物左右各有一个可行的绕行方案），它只会停留在出发时所在的那个"盆地"里——这就是同伦类问题。

另一条完全不同的思路是采样：平行地掷出一大批候选控制序列，每条都前向仿真出一条轨迹，再按轨迹代价做加权平均——代价低的方向占主导，迭代几轮就向"整体上更优的方向"聚拢。这就是 MPPI。它不依赖梯度，所以不受"只能走下山路"的限制。

我之前在这个仓库里引入过 MPPI，用法是给 FDDP 当播种器，先用采样探出大方向，再把最优的那条序列交给梯度法精修。当时有两个期望：

- 主要期望是让它自己发现"台阶前速度不够，应该先后退取得加速空间"这个全局最优。这正是第 3 章那段架构演进的起点。
- 次要期望和暖启动有关。MPC 每拍平移上一帧的解作为初值，如果某一帧的 rollout 贴到了移动障碍物上，下一帧的初值就带着这个偏差继续走——轨迹会被障碍物"推着走"，而不是从障碍物后面绕过去。采样有机会跳到那个绕行的同伦类里。

现在这两个期望都不在了，MPPI 的代码已经从仓库里移除。原因分别是：

主要期望被证明找错了层次。"先后退再冲上去"这个行为的缺失，根因是搜索层的状态里没有速度维度，那条轨迹压根不在全局路径里，指望一个局部的 local planner 去做不如在 global planner 搜索层直接处理。搜索层扩成"位置 + 朝向 + 速度"之后，这个行为直接就被搜出来了，控制层不再需要任何局部搜索能力。

次要期望被判断为不值得。被障碍物推着走确实会发生，但它是自限的：车偏出去之后，路径跟踪的代价会把它拉回来，很快就能恢复。为了让它少偏一点而在控制层常驻一套采样求解器（以及它带来的采样噪声、参数和算力开销），收益不抵成本。这是一个"能优化，但没必要优化"的项。

所以这段经历真正的教训不是"MPPI 不好用"——它是一个有用的工具，我用的方式也是它被推荐的用法之一。教训是：**引入一个机制之前，要先确认你指望它解决的问题，根因确实在这一层。** 我的主要期望根因在上游，次要期望根本不构成问题。两个期望都站不住，那么这个机制无论多合理都留不下来。

顺带说清楚，我确实没有把 MPPI 本身调到最好——温度、噪声协方差、采样数这些都只是粗调过。所以上面的结论是"在我的场景里它要解决的问题不成立"，不是"它在这类问题上不行"。如果你的规划层就是纯几何的、拓扑必须靠控制层自己找，那 MPPI 的价值可能会比在我这里高得多。

这里想让你理解的是那个更普适的分工：**发散找方向（该绕左还是绕右）和收敛求精度（在选定方向上打磨）是两件不同的事，需要不同的机制。** 采样和梯度是这个分工在求解器尺度上的一种实现，第 3 章"搜索负责发散、优化负责收敛"是它在架构尺度上的另一种实现。而本仓库最后选择的是后者——把发散的职责放在搜索层，控制层就只需要一个纯粹的梯度求解器。这个分工本身是普适的，但它落在哪一层，是你的架构选择。

### 控制器接入真实系统的三条接缝

前面讲的都是"把控制问题建模好"。但控制器不是一个孤立的优化问题，它嵌在一条有时钟、有接口、有状态的链路里。有三条接缝，只要你的架构满足对应的前提，问题就一定会出现，和运气无关。这些不是"我碰巧遇到的坑"，是可以从架构前提推导出来的必然结果——建议设计的时候注意。

#### 接缝一：你的控制拍和反馈拍是两个独立时钟吗？

典型架构是：控制器挂在一个 20 Hz 的定时器上，底盘状态也以 20 Hz 发过来。看起来很匹配，实际上这是两个互不同步的时钟。如果运气不好，底盘状态和控制器的相位一致，可能遇到：某一拍定时器触发时新状态还没到（只能沿用旧值），下一拍又积压了两帧。结果是控制的有效频率在标称值的一半到一倍之间随机游走，而且依赖状态历史的模块（观测器）会拿到重复或跳跃的序列。

这个问题最难的地方在于症状离根因很远，且发生概率较低不易复现。你看到的是"控制时好时坏"，很容易去怀疑参数、怀疑模型，而根因在时钟。

解法很干净：别让控制器自己计时，让反馈来驱动控制。每收到一帧底盘状态就跑一拍控制，两个时钟就变成了一个，相位混叠在物理上不可能发生。

#### 接缝二：你直接输出速度指令吗？

如果 MPC 的控制量就是速度 (v, ω)，那么相邻两拍的输出之间没有任何机制阻止它跳变——优化器完全可以这一拍要 0.5 m/s，下一拍要 2.5 m/s，因为在它的模型里这是允许的。而下位机常有指令加速度限制，它跟不上，实际响应和你的预测就此分道扬镳。

比较好的解法是换一个建模视角：把指令本身当作状态，把指令的变化率当作控制量。这样"下位机的速率限制"就从一个你无法表达的物理事实，变成了优化问题里一个标准的控制量边界——优化器在求解时就已经知道自己不能突变。附带的好处是，下位机的速度上下限和启动时"指令与实测不能差太多避免过冲"这类要求，都可以一起折进这个边界里。

当然也有另外一种解法，就是在优化器里使用软约束限制指令变化超限。不过这种方案本质上是把一个硬约束（下位机限幅）变成一个软约束（代价项），优化器会在"代价下降"和"不违反限幅"之间权衡——它可能会选择违反限幅来换取更低的代价。你可以调权重让它不违反，但这只是一个经验值，不能保证在所有场景下都成立。

这条的普适版本是：当你发现"输出不该这样但我没法禁止它"时，考虑把那个量提升为状态，让你想施加的限制变成对新控制量的约束。

#### 接缝三：你的模型里有观测器估计的隐状态吗？

如果有，那么观测器需要持续跟踪，即使控制器此刻没在输出指令。这一点很容易被写错，因为直觉上"控制器没工作，观测器也不用工作"。但实际情况是：机器人在脱困、在保持、在等待恢复的这几秒里，物理状态一直在变；如果观测器跟着停摆，等控制器重新接管时，它拿到的隐状态是几秒前的——用一个过期的初始状态去做预测，第一拍就错。

不过这里有个必须讲清楚的细节，否则会被"观测器要一直更新"这句话误导：观测器能不能更新，取决于它的输入是否可信，而不是控制器是否活跃。观测器靠"我发了什么指令 → 车实际怎么响应"这个对应关系工作。如果底盘此刻根本不接受导航指令（比如腿正在做动作、或者比赛没开始），那么"指令-响应"这个链条是断的，硬要更新只会让它学到错误的东西——这种情况下正确的动作是显式重置观测器。

所以准确的表述是：**观测器的更新条件应该由"输入是否可信"决定，而不是由"控制器在哪个状态"决定。** 这两者经常不一致，而按后者写是错的。

### 需要了解的关键知识点

- **经典控制基础**：PID、纯追踪等经典路径跟踪算法、传递函数与状态空间模型、能控性/能观性、李雅普诺夫稳定性、LQR——控制论的第一课，也是理解后面一切的前置
- **动态规划与最优控制**：Bellman 方程、把整条轨迹优化分解成逐段递推——MPC 与 DDP 的理论源头
- **MPC 思想**：模型 + 预测 + 滚动时域——理解"每拍重算、只执行第一步"为什么是鲁棒性的来源，而不是妥协
- **DDP 家族**：iLQR / DDP / FDDP 的区别、为什么马尔可夫链式结构让它们比通用 NLP 快——"用结构换速度"的求解器思维
- **MPPI**：采样式 MPC 的原理（平行采样 + 代价加权）、它与梯度式 DDP 的互补关系——本仓库最终没有采用它，但理解"什么时候需要在控制层做发散搜索"很有价值
- **观测器**：Luenberger / Kalman 怎么从可测输出重建不可测状态——模型里有测不到的物理量时怎么办
- **系统辨识**：最小二乘、ARX / LPV 模型是什么——理解"为什么模型要数据辨识而不是手搓"
- **离散化**：ZOH 精确离散与朴素积分的区别——快极点系统上朴素积分会直接发散，这是模型进控制器前的最后一关
- **非最小相位系统**：为什么轮腿速度会"先反向再跟上"——认识你的被控对象

### 本仓库的体现

`nav_executor` 的执行器：LPV 辨识模型 + 观测器 + 自研 FDDP 求解器 + 命令率控制 + 名义/辅助两层结构。控制拍由底盘反馈驱动，不设独立定时器。三个问题共用一个求解器：跟随（Follow）、停车（Stop）、保持（Hold）。详见 DESIGN.md 第 4.3 节。

## 第 5 章 出错了怎么办：鲁棒性问题

> 这一章推倒：A5 一切按计划发生，不会卡住、贴障、失效。

### 这一章解决什么问题

前面四章假设一切顺利。但真实世界的导航会失败——卡住、贴障、没进展。怎么办？这一章讲的是"承认失败并优雅地处理失败"。

### 怎么想这个问题

首先要有一个心态转变："正常跟随会失败"是默认假设，不是悲观假设。车轮会卡在台阶边缘（有指令、没位移）、机器人会贴进障碍（当前位置已经危险）、路径会变得不可行（比如台阶被障碍物遮挡）。这些不是 bug，是物理世界的一部分。

然后要认清一个比赛机器人才有的奖惩结构：撞一下、掉下去，可能还能继续打；但卡死在原地，就彻底失去作用。所以对比赛机器人，鲁棒性的第一性需求不是"安全"，而是活性——系统必须永远"活着"：出错可以，但必须还能动、还能继续前进。

形式化一点说，系统性质分两类：安全性（safety，"坏事情永远不会发生"）和活性（liveness，"好事情终将发生"）。工业机器人出错可以停在安全状态等人来救（fail-safe）；比赛机器人不行——停车等待本身就是死亡。设计目标应该是 fail-live：就算出错，也出得还活着。记住这条主线，下面三条推论都是它的直接展开。

#### 推论 1：每个状态都必须有出口

状态机的死锁几乎都是同一个病根：某个状态的退出条件依赖一个可能永远不满足的信号——"等路径恢复"，但路径可能永远不会恢复了。所以设计状态机时，对每个状态问三个问题：怎么进？正常怎么出？异常怎么出？第三个问题最容易漏，漏了就是潜在的不动点。脱困、危险恢复这类状态存在的意义不是"锦上添花的容错"，而是死锁逃生通道——它们保证任何状态下，系统都有一条"还活着"的路可走。

这个问题特别容易在维护和扩展过程中出现。人在加新状态时，注意力天然全在正常流程上——因为你是为了实现某个正常行为才加它的。异常出口不是忘了写，是当时根本没想到要写。所以这件事得靠机制，不能靠记性：每加一个状态，强制回答那三个问题，尤其是第三个。

#### 推论 2：主动检测是打破不动点的机制，超时是保底出口

既然任何状态都可能因为条件不满足而卡住，就不能"等失败被发现"——要主动检测：无进展吗？卡住了吗？危险吗？

但这里有一条必须成对的纪律：**检测必须和恢复成对出现**。只检测不恢复，判据反而制造新的死锁——检测到卡住，却没有地方可去，还是卡着。而超时的本质是"即使条件永远不满足，系统也能强制离开当前状态"——它给每个状态装了一个保底出口。

判据与状态机的解耦（判据在外面算好、状态机只做决策转换）正是让这条纪律可行的结构：判据是"自知"的传感器，状态机是处理显式失败的决策器，两者各自独立演进、分别测试——判断是事实，决策是选择。

#### 推论 3：恢复链本身必须收敛

恢复逻辑自己也可能死锁——倒车 → 还是危险 → 再倒车 → 还是危险，循环往复。所以恢复链要像主状态机一样认真设计：

第一，先物理脱困，再重新规划——卡住的位置起点不可行，直接重规划必然失败，必须先动起来（倒车、找安全点），恢复安全后再触发重规划。第二，恢复最好有终态——连续多次相近位置失败可以取消任务换个路径走，避免无限失败重试。第三，不要触发不必要的恢复——比如动态障碍挡路，用 MPC 绕开就行，不该触发恢复流程，否则恢复链被无关事件反复打断，本身就变成一种抖动。

到这里可以做一层澄清：**活性是硬约束，安全是软约束。** 安全约束（台阶动作不可打断、别撞墙）仍然要有，但它永远不能以失去活性为代价——卡住时宁可倒车撞到点什么，也不能卡死。错误代价的排序应该是：**卡死 > 致命 > 慢/绕**。台阶动作的"不可打断"看似是安全约束，其实并不违反活性：动作进行中系统还在动，只是不被打断。把"活着"设为硬约束、"安全"设为软约束，设计时的优先级就清楚了。

还有一个值得单独想的问题：控制权仲裁。底盘同时被多个需求争抢：正常跟随、停车、小陀螺（对枪时的生存需求）、台阶动作（进行中不应被打断）。谁有权动路径？什么时候可以抢占、什么时候不可打断？这个需要你根据具体情况自行设计。

最后，如果你觉得状态机繁琐，还有一个组织结构上的选择值得知道：行为树。状态机擅长表达强时序的流程（"必须经过这个状态才能到那个状态"），但状态一多转换图就爆炸，失败处理要单独画边。行为树把行为组织成树，每拍从根节点向下"tick"——选择节点按优先级尝试、顺序节点要求全部成功，失败回退是树结构自带的语义。它在游戏 AI 和 Nav2 里是主流，适合"任务流"：复杂任务拆成可复用的小行为，失败路径长在结构里。而状态机更适合"时序流"：台阶的"准备 → 锁定 → 执行 → 释放"有不可打断区间，用显式转换表达最清楚。所以不是谁更好，而是你的行为是任务流还是时序流——想清楚再选，比选了再想更省事。

### 需要了解的关键知识点

- **活性与安全性**：系统性质的两个经典分类——"好事情终将发生" vs "坏事情永远不会发生"——比赛机器人优先保证活性的理论基础
- **有限状态机（FSM）**：状态机设计模式、Moore / Mealy 的形式语义、状态图（statecharts）与层次状态机——组织"时序流程"型行为决策的经典结构
- **行为树（BT）**：Selector / Sequence 的语义、与状态机的取舍——组织"任务流"型行为决策的另一种结构（游戏 AI、Nav2 的主流选择）

### 本仓库的体现

`nav_executor` 的执行器：FSM 只管状态转换（正常/台阶/小陀螺/脱困/危险恢复），所有判据在监控组件里算好，底盘失能则在状态机之前就被拦掉；恢复链为"倒车（带超时保底）→ 危险恢复 → 安全保持 → 重规划"；仲裁先定控制归属，再导出"导航访问权限"三态（可用 / 锁当前 / 撤销）决定谁有权动路径。详见 DESIGN.md 第 4.2 节和第 3.1 节。

## 第 6 章 跨地形：轮腿的终极问题

> 这一章推倒最后一条：A6 地面是平的，"能走/不能走"是二值的。

### 这一章解决什么问题

对普通机器人，台阶是障碍——绕开。对轮腿机器人，台阶是要以特殊姿态跨过去的。这个区别，是整个系统设计的引爆点：它迫使地图、规划、控制、接口全部升级。

### 怎么想这个问题

通用导航可以直接把台阶当障碍，因为它们的轮子过不去。轮腿要跨，所以必须把台阶从"障碍"里拎出来，承认它是第三种东西：带动作的地形。

这里值得先想清楚一个更深的问题：为什么"障碍/空地"这个二分法装不下台阶？

第 2 章说过，地图的本质是表达"空间位置 → 通行代价"的函数——普通代价地图把通行性写成位置的函数：这里能不能走。但台阶恰恰证明了这个抽象不够：能不能过这个台阶，不取决于你在哪，而取决于你以什么状态经过——朝向（要正对台阶）、速度（要在速度窗里，快了翻车、慢了上不去）、底盘模式（要切换到上台阶对应模式）。

所以方向场、速度窗、能力档这些看似"台阶特有的花活"，深层含义其实是：

**地图从"位置 → 代价"的函数，进化为"位置 × 状态 → 代价"的函数**。普通 costmap 回答"这里能不能走"，语义地图回答"以什么状态走才可能"——多出来的每个维度（朝向、速度、模式），都对应一个真实会翻车的约束。想清楚这一点，你就明白地形语义不是锦上添花，而是跨地形导航的地基。这也正是第 3 章那段架构演进的根源：一旦通行性依赖状态，搜索的状态空间里就必须包含那些状态，否则可行的拓扑根本无法被表达。

一旦承认这一点，整个系统都要跟着变——而且是从最底层变起：

- **地图要有语义**：标注地形类型和穿越方向（哪边是"上"、哪边是"下"），而不是只标"能不能走"。
- **规划要有约束**：要以某个速度带通过（飞坡时慢了可能掉坑里，下二级台阶时快了可能翻车）、要冲着台阶（方向对齐）、要提前满足条件（起跳点可能在前面，不是物理边缘）。
- **接口要给底盘"模式"**：跨台阶是底盘的动作，导航不能只发速度指令，还要提前告诉底盘"准备跨台阶"，底盘切换到对应模式。
- **执行要有生命周期**：提前准备 → 模式切换 → 动作开始 → 完成释放。因为底盘动作期间可能独占机构，导航的命令要配合它的节奏。

这里有一个设计思路：把"跨台阶需要什么"这个复杂的意图，在规划期就编译成一份可执行的数据"契约"——台阶在哪、什么模式、速度窗是多少、哪段不可打断——然后执行层只消费这份契约，不再重新解释地图。这么做有三个层面的理由。

第一，执行层不应该"认识"台阶——知识放在数据里，而不是代码里。想象一下，如果执行层要自己解释地图，它就得内置一堆判断规则："前方是台阶 → 切换跳跃模式 → 速度保持在 1.6~2.2 m/s"。这些规则一旦写死在代码里，场地一变（换地图、改标注、调速度窗），就得改代码重新编译。

契约的做法恰好相反：规划器把"台阶在哪、用什么模式、速度窗多少、哪段不可打断"全部算好，写进一份数据；执行层只读数据、照着做——它不需要知道有台阶，甚至约束本身也可以不是台阶（比如飞坡、过洞）。场地变了，改的是地图和标注，代码一行不用动。这就是"执行层不解释地图"的真正含义：解释地图，等于把知识写死在代码里。

第二，计算频率应该跟随数据的生命周期。台阶标注完全依赖全局路径：路径不变，标注就不会变。既然输入不变、输出也不会变，就没有理由每拍重算——把它放在规划期，路径一变它自然跟着重新编译；放在执行期每拍重算，纯粹是浪费。

这其实是一条贯穿整个系统的暗线：静态地形赛前离线标注（输入赛前已知）、动态障碍在线检测（依赖实时点云）、台阶契约在规划期固定（生命周期依赖全局路径）、MPC 每拍求解（依赖底盘状态）——**"在哪里算、多久算一次"，由输入以什么频率变化决定**。这条判断的适用范围远超台阶：任何时候你觉得某个计算"有点重"，先别急着优化它，先问它的输入多久变一次。

第三，契约要用"区域"而不是"点"来表达——硬约束会杀掉可行性。这一条是我试错换来的。最初的台阶契约里，我给每个台阶入口生成一个硬路点：位置和朝向都固定，要求轨迹必须精确穿过它。动机很朴素——"入口必须对齐，那就把它钉死"。

结果是轨迹质量被锁死在搜索层的粗离散拓扑上：硬路点的位置来自搜索的格点，它未必是最优的入口位置，但优化器无权调整，只能在它周围勉强凑出一条曲线。更糟的是它经常让问题无解——路点的朝向和前后段的几何要求冲突时，优化器找不到任何满足全部硬约束的轨迹，直接失败。

改法是把硬路点全部换成代价场：不再规定"必须从这一点以这个朝向进入"，而是"在台阶这一整个区域里，方向要尽量对齐、速度要尽量落在窗内、路径要尽量直"。优化器于是可以自由选择落点，在满足物理要求的前提下找一条它自己觉得最顺的路——助跑段反而变得更自然了。

这和第 3 章"裁决可行性用硬约束、引导质量用软约束"是同一条规律的另一面：那里讲的是该用硬约束的地方，这里讲的是用错地方的后果。判断标准还是层职责：入口对齐是"质量"还是"可行性"？它是质量——对齐得差一点只是动作难看或者失败率高一点，不是物理上不可能。所以它属于软约束。真正该硬的是"这个台阶的这个方向能不能过"，那个判断在搜索层已经做完了。

一句话总结：**规划层负责想清楚，执行层负责执行清楚** ——这是复杂需求下保持系统清晰的唯一办法。

再往深看一层：方向场、速度窗、能力档、生命周期——这些不是台阶特有的花活，而是"把世界约束编进规划"的一般方法。你的机器人需要什么约束（无论是腿车上下台阶、飞坡、跳高地，还是饼车过洞），就把它编成地图语义和规划代价。这套方法可以迁移到任何非平面地形、任何特殊底盘。

### 本仓库的体现

语义地图（地形标签 + 方向场）在 `map_server`；规划期的台阶约束（速度窗、方向对齐、助跑区）在 `nav_executor` 的规划器；执行期的生命周期契约（沿弧长的一串里程碑 prepare → active → commit → enter → exit → release，车体越过它们时依次进入 PREPARING / ARMED / COMMITTED 三个子状态，其中 COMMITTED 锁定路径）在执行器。详见 DESIGN.md 第 3.3 节和第 4.2 节。

## 结尾：怎么继续探索

这份指南到这里就结束了。最后说四件事。

### 第一，把"为什么"当成你的习惯

读任何系统的代码时，先问"这里为什么这样设计"，再去验证你的猜测。这个仓库里每个"看起来过度设计"的地方，背后都有一个"当初那样不行"的故事——你不必知道故事，但一定要习惯提问。好设计的共同点不是"用对了什么算法"，而是每个选择都有明确的动机。

一个具体的提问工具：拿第 0 章那张假设表去问。看到一处复杂的机制，问它在对抗哪条假设的倒下。如果答不上来，它可能真的是多余的。

### 第二，推荐的探索路径

先跑 DEMO.md 建立直觉（看东西动起来比读十篇文档有用）→ 按数据流顺序读 DESIGN.md（建立系统心智模型）→ 需要深入时再打开对应包名下的代码（或者直接问 coding agent）。知识类的问题，随时丢给 LLM 问，本文的"知识点"清单就是你的提问菜单。

如果你想更快地理解"为什么长这样"，还有一个办法：去看 git 历史里被删掉的东西。被推翻的设计比最终的设计更能说明问题——它们解释了为什么现在这版要这么写。

### 第三，弯路有形状，认出形状比记住路线更有用

我在正文里散落着讲了几段弯路。它们的具体内容对你未必有用（你的机器人和需求都不一样），但它们的形状是可迁移的。我自己踩过的弯路，归纳起来反复是这么几种：

**在错误的层次解决问题。** 现象出现在下游，根因在上游的表示能力不足。我在控制层折腾了很久去补一个搜索层缺少速度维度造成的缺口。自查方式：先确认上游给的候选集里有没有你想要的解。

**用重方案替代已经够用的轻方案。** 我把规划拆成三个包想引入一个中间层，两天后全部回滚；我做过硬性的几何验收，当天就删掉（真实地图上拒收率高到不可用）；我做过自动调参，最后删了。自查方式：新方案要替代的那个东西，现在到底哪里不够用？说不出具体的不足，就先别动。

**跳过前置条件就上高级方案。** 想直接在栅格上估速度，跳过"聚类成对象"和"帧间关联"，结果预测全是噪声。自查方式：这个方案的输入，需要什么质量的上游产物？那个产物现在有吗？

**加了新状态但没设计它的失败路径。** 助跑状态的死循环。自查方式：新状态怎么进、正常怎么出、异常怎么出。

**混淆变量没控制住就做 A/B。** 我曾经在真车上反复切换两种模型选型，来回三天没有结论——因为每换一次模型都要重调控制器权重，两个变量一起动，什么也证明不了。自查方式：这次对比里，除了我想比较的那一项，还有别的东西在变吗？能不能先离线比？

**用硬约束表达本该软的东西。** 硬路点锁死了轨迹质量、几何硬验收拒收一切。自查方式：这个要求是"可行性"还是"质量"？质量用软约束。

你不一定会重复我的弯路，但你大概率会在某个地方犯下同一种形状的错。

### 第四，也是最重要的：不要怕走弯路

这份指南里的每一个设计，都是从最朴素的版本开始，被真实问题一步一步逼出来的。第一个版本能有多简单就多简单——先让它跑起来，再让它跑得好。你会踩坑，会推翻自己的设计，会 ~~让 agent~~ 写出自己都看不懂的代码——这些都太正常了。

**走弯路不是失败，是探索的必经之路。** 保持好奇，保持怀疑，一定要动手去做。
